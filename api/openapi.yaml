openapi: 3.1.0
info:
  title: MyInvoice.cz REST API
  version: "1.0"
  summary: Veřejné REST API pro automatizaci fakturace
  description: |
    Veřejné API MyInvoice.cz pro integraci s e-shopy, CRM, automatizačními platformami
    (Make, Zapier) a vlastními skripty.

    ## Autentizace

    Použij Personal Access Token (PAT) v hlavičce `Authorization`:

    ```
    Authorization: Bearer mi_pat_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
    ```

    Token vytvoříš v MyInvoice → **Nastavení → API tokeny**. Plain-text se zobrazí
    pouze jednou při vytvoření — ulož si ho. Pokud token ztratíš, odvolej ho a vytvoř
    nový.

    Token má:
    - **scope** — `read` (pouze GET) nebo `read_write` (všechny operace).
    - **supplier** — pokud je token bound na konkrétního supplier-a, všechny operace
      se odehrávají v jeho kontextu. Pokud je `supplier` `null`, klient může
      přepínat přes hlavičku `X-Supplier-Id: <id>`.
    - volitelnou expiraci.

    ## Verzování

    Stabilní veřejná cesta začíná `/api/v1/`. Pokud přidáme nekompatibilní změnu,
    půjde do `/api/v2/` — `/api/v1/` zůstane funkční.

    Každá response vrací hlavičku `X-API-Version: 1`.

    ## Rate limity

    Defaultně **600 requestů / minutu / token**. Při překročení vrátíme `429 Too Many Requests`
    s hlavičkou `Retry-After` (sekundy).

    ## Chyby

    Všechny chyby mají formát:

    ```json
    { "error": { "code": "validation_failed", "message": "..." } }
    ```

  contact:
    name: MyInvoice.cz
    url: https://myinvoice.cz
  license:
    name: AGPL-3.0
    url: https://www.gnu.org/licenses/agpl-3.0.html

servers:
  - url: /
    description: Aktuální origin
  - url: http://localhost:8080
    description: Docker

security:
  - BearerAuth: []

tags:
  - name: System
    description: Health-check, verze, dokumentace
  - name: Auth
    description: Identita aktuálního klienta
  - name: Tokens
    description: Správa API tokenů (jen ze session — webového UI)
  - name: Clients
    description: Klienti / odběratelé
  - name: Invoices
    description: Faktury
  - name: Projects
    description: Zakázky (vázané na klienta, jednotka pro reporting a fakturaci)
  - name: WorkReports
    description: Výkaz víceprací (po hodinách) k faktuře — vytvoření, schválení zákazníkem, PDF
  - name: InvoiceComms
    description: Email faktury, upomínky, archiv vystavených PDF, přílohy
  - name: InvoiceOps
    description: Pokročilé operace s fakturami (storno, klonování, hromadné akce, proforma→final)
  - name: Lookup
    description: ARES (CZ obchodní rejstřík), VIES (EU VAT validation)
  - name: Reports
    description: Dashboard summary, CSV exports, activity log
  - name: Bank
    description: Bankovní výpisy a párování plateb
  - name: Settings
    description: Konfigurace dodavatele a per-supplier overrides codebooku (read-only přes API)
  - name: Codebooks
    description: Číselníky (země, měny, DPH sazby, jednotky)
  - name: Documents
    description: Dokumenty — úložiště souborů, složky, ZFO datové zprávy, fulltext, párování s entitami
  - name: Purchase invoices
    description: Přijaté faktury (náklady) — evidence, položky, DPH odpočet, export, párování záloh
  - name: Recurring
    description: Pravidelné (opakované) fakturace — šablony, jejich rozvrh a generování faktur
  - name: CRM
    description: CRM dashboard — KPI, aging, koncentrace, cash-flow, action-items
  - name: Tax
    description: Daňový optimalizátor (srovnání režimů, predikce ročních limitů)
  - name: Logbook
    description: Kniha jízd — vozidla, jízdy, tankování a daňové souhrny

paths:
  /api/v1/health:
    get:
      tags: [System]
      summary: Health-check
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean, example: true }

  /api/v1/version:
    get:
      tags: [System]
      summary: Verze aplikace
      security: []
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  version: { type: string, example: "4.3.1" }

  /api/v1/auth/api-me:
    get:
      tags: [Auth]
      summary: Identita aktuálního klienta
      description: |
        Vrátí informace o uživateli a supplier-ovi, pod kterým token / session
        operuje. Slouží jako connection-test pro integrace.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ApiMe' }
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/clients:
    get:
      tags: [Clients]
      summary: Seznam klientů
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - in: query
          name: q
          description: Fulltextové vyhledávání (název firmy / IČO)
          schema: { type: string }
        - in: query
          name: filter[archived]
          description: "1 = jen archivované, 0/chybí = jen aktivní"
          schema: { type: boolean, default: false }
        - in: query
          name: sort
          description: "Řazení (default: name)"
          schema: { type: string, default: name, enum: [name, created_at, -created_at] }
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 5, maximum: 200, default: 50 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  clients:
                    type: array
                    items: { $ref: '#/components/schemas/Client' }
                  pagination: { $ref: '#/components/schemas/Pagination' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'
    post:
      tags: [Clients]
      summary: Vytvoří klienta
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ClientInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Client' }
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/InsufficientScope'

  /api/v1/clients/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Clients]
      summary: Detail klienta se statistikami
      description: |
        Vrátí klienta + odvozené metriky pro detail stránku:

        - `projects` — seznam zakázek klienta,
        - `invoices_count` — celkový počet faktur,
        - `revenue_by_month` — obrat po měsících za posledních 24 měsíců (VAT-aware),
        - `revenue_by_year` — obrat po letech (VAT-aware),
        - `revenue_by_project` — obrat agregovaný per zakázka pro graf „Obrat podle zakázek"
          (faktury bez `project_id` se agregují pod `project_id: null`),
        - `unpaid_summary` — nezaplaceno / po splatnosti per měna.

        Obraty zahrnují vystavené faktury + dobropisy (`issued`, `sent`,
        `reminded`, `paid`), zálohy a koncepty se nepočítají. Pro plátce DPH
        se používá `total_without_vat`, pro neplátce `total_with_vat`.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ClientDetail' }
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Clients]
      summary: Aktualizace klienta
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ClientInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Client' }
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Clients]
      summary: Smazání klienta (pokud nemá vystavené doklady)
      responses:
        '200':
          description: OK
        '409':
          description: Klient má vystavené doklady, použij archivaci
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/clients/{id}/archive:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Clients]
      summary: Archivuje klienta
      description: |
        Nastaví `archived_at = NOW()`. Archivovaný klient se nezobrazuje
        v defaultním seznamu, jeho historické faktury zůstávají dohledatelné.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Client' }

  /api/v1/clients/{id}/unarchive:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Clients]
      summary: Vrátí archivovaného klienta zpět do aktivních
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Client' }

  /api/v1/invoices:
    get:
      tags: [Invoices]
      summary: Seznam faktur (grouped by month)
      description: |
        Vrací faktury aktuálního supplier-a, seskupené po měsících podle `issue_date`.
        Filtry se předávají jako `filter[key]=value` (deep object). `status` a `type` mohou
        být čárkou oddělené seznamy (`filter[status]=draft,issued`).
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - in: query
          name: q
          description: Fulltextové vyhledávání (varsymbol, klient, popis)
          schema: { type: string }
        - in: query
          name: filter[status]
          description: Stav faktury — lze čárkou oddělit více hodnot
          schema:
            type: string
            example: "draft,issued"
        - in: query
          name: filter[type]
          description: Typ dokladu — lze čárkou oddělit více hodnot
          schema:
            type: string
            example: "invoice,proforma"
        - in: query
          name: filter[client_id]
          schema: { type: integer }
        - in: query
          name: filter[project_id]
          schema: { type: integer }
        - in: query
          name: filter[year]
          description: Rok (issue_date)
          schema: { type: integer, example: 2026 }
        - in: query
          name: filter[month]
          description: Měsíc 1–12 (vyžaduje filter[year])
          schema: { type: integer, minimum: 1, maximum: 12 }
        - in: query
          name: filter[date_from]
          description: Datum vystavení od (YYYY-MM-DD)
          schema: { type: string, format: date }
        - in: query
          name: filter[date_to]
          description: Datum vystavení do (YYYY-MM-DD)
          schema: { type: string, format: date }
        - in: query
          name: filter[currency]
          description: Kód měny (CZK, EUR, …)
          schema: { type: string, example: CZK }
        - in: query
          name: filter[unpaid_only]
          description: Pouze nezaplacené (issued + neuhrazené)
          schema: { type: boolean }
        - in: query
          name: filter[overdue]
          description: Pouze po splatnosti
          schema: { type: boolean }
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 5, maximum: 200, default: 50 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoices:
                    type: array
                    items: { $ref: '#/components/schemas/Invoice' }
                  pagination: { $ref: '#/components/schemas/Pagination' }
    post:
      tags: [Invoices]
      summary: Vytvoří draft faktury
      description: |
        Vytvoří fakturu ve stavu `draft`. Pro vystavení (přidělení čísla, zápis do
        DB účetně) volej `POST /api/v1/invoices/{id}/issue`.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/InvoiceInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }
        '400':
          $ref: '#/components/responses/ValidationFailed'

  /api/v1/invoices/preview-varsymbol:
    get:
      tags: [Invoices]
      summary: Náhled budoucího čísla faktury
      description: |
        Vrátí, jaké číslo (varsymbol) by faktura dostala při vystavení, podle
        aktuální `invoice_number_template` supplier-a a stávajícího counteru —
        **bez inkrementace**. Slouží pro náhled v editoru.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - in: query
          name: type
          schema:
            type: string
            enum: [invoice, proforma, credit_note]
            default: invoice
        - in: query
          name: issue_date
          description: Datum vystavení (YYYY-MM-DD). Default = dnes.
          schema: { type: string, format: date }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  varsymbol:
                    type: string
                    example: "JD2026-01"
                    description: Prázdný řetězec pokud supplier nemá nastavenou template.
                  has_template: { type: boolean }
        '400':
          description: Chybí zvolený supplier nebo neplatný typ
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Invoices]
      summary: Detail faktury
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Invoices]
      summary: Aktualizace draftu
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/InvoiceInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }
        '409':
          description: Faktura už není draft (vystavená/storno) — nelze editovat
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [Invoices]
      summary: Smazání draftu
      responses:
        '200':
          description: OK
        '409':
          description: Vystavenou fakturu nelze smazat (použij storno)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/issue:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Invoices]
      summary: Vystaví fakturu (z draftu)
      description: |
        Přidělí číslo dle nastavení supplier-a, archivuje PDF snapshot. Po vystavení
        nelze měnit položky/částky.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }

  /api/v1/invoices/{id}/mark-paid:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Invoices]
      summary: Označí fakturu jako uhrazenou
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                paid_at:
                  type: string
                  format: date
                  description: Datum platby (default = dnes)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }

  /api/v1/invoices/{id}/payments:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Invoices]
      summary: Evidované platby faktury
      description: |
        Seznam plateb (částečných úhrad) faktury nebo zálohové faktury + souhrn:
        `paid_total`, `remaining` (= `amount_to_pay - paid_total`) a odvozený
        `payment_status` (`unpaid` / `partially_paid` / `paid` / `overpaid`).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  payments:
                    type: array
                    items: { $ref: '#/components/schemas/InvoicePayment' }
                  paid_total: { type: number, format: float }
                  amount_to_pay: { type: number, format: float }
                  remaining: { type: number, format: float }
                  payment_status:
                    type: string
                    nullable: true
                    enum: [unpaid, partially_paid, paid, overpaid]
    post:
      tags: [Invoices]
      summary: Zaeviduje (částečnou) úhradu faktury
      description: |
        Přidá platbu v měně faktury. Pokud suma plateb pokryje `amount_to_pay`
        (tolerance 0,05), faktura se označí jako zaplacená (`paid_at` = datum
        poslední platby). Stávající `mark-paid` je zkratka „platba na zbytek".
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                amount:
                  type: number
                  format: float
                  description: Částka platby v měně faktury (> 0)
                paid_on:
                  type: string
                  format: date
                  description: Datum platby (default = dnes)
                variable_symbol: { type: string, nullable: true, maxLength: 20 }
                bank_reference: { type: string, nullable: true, maxLength: 120 }
                note: { type: string, nullable: true, maxLength: 255 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoice: { $ref: '#/components/schemas/Invoice' }
                  payments:
                    type: array
                    items: { $ref: '#/components/schemas/InvoicePayment' }
                  payment: { $ref: '#/components/schemas/InvoicePayment' }
                  became_paid: { type: boolean }
                  remaining: { type: number, format: float }
        '409':
          description: Neplatný stav/částka (draft, storno, dobropis…)

  /api/v1/invoices/{id}/payments/{paymentId}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
      - in: path
        name: paymentId
        required: true
        schema: { type: integer, format: int64 }
    delete:
      tags: [Invoices]
      summary: Smaže evidovanou platbu
      description: |
        Platbu navázanou na bankovní transakci nelze smazat (409) — ruší se přes
        unmatch v detailu výpisu. Platbu s vystaveným daňovým dokladem k přijaté
        platbě lze smazat až po smazání/stornu dokladu. Pokud po smazání přestane
        být doklad pokrytý, vrátí se ze stavu `paid` zpět (`sent`/`issued`).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoice: { $ref: '#/components/schemas/Invoice' }
                  payments:
                    type: array
                    items: { $ref: '#/components/schemas/InvoicePayment' }
                  became_unpaid: { type: boolean }
                  remaining: { type: number, format: float }
        '409':
          description: Platba je zamčená (bankovní vazba / daňový doklad)

  /api/v1/invoices/{id}/payments/{paymentId}/tax-document:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
      - in: path
        name: paymentId
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Invoices]
      summary: Vystaví daňový doklad k přijaté platbě (DRAFT)
      description: |
        K platbě **zálohové faktury** vystaví koncept daňového dokladu k přijaté
        platbě (`invoice_type = tax_document`, § 28 odst. 2 ZDPH): DUZP = datum
        platby, DPH shora koeficientem (§ 37), částka rozdělená mezi sazby DPH
        proformy poměrně. Jen plátce DPH a ne-reverse-charge plnění. Idempotentní —
        existující nestornovaný doklad k platbě vrátí beze změny.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  tax_document_id: { type: integer, format: int64 }
                  payments:
                    type: array
                    items: { $ref: '#/components/schemas/InvoicePayment' }
        '409':
          description: Podmínky nesplněny (neplátce DPH, reverse charge, není proforma)

  /api/v1/invoices/{id}/pdf:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Invoices]
      summary: PDF faktury
      responses:
        '200':
          description: PDF stream
          content:
            application/pdf:
              schema: { type: string, format: binary }

  /api/v1/projects:
    get:
      tags: [Projects]
      summary: Seznam zakázek
      description: |
        Vrátí stránkovaný seznam **neArchivovaných** zakázek v rámci aktuálního
        supplier-a. Filtrování přes `filter[status]` a `filter[client_id]`.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - in: query
          name: filter[status]
          schema:
            type: string
            enum: [active, paused, closed]
        - in: query
          name: filter[client_id]
          schema: { type: integer, format: int64 }
        - in: query
          name: sort
          schema:
            type: string
            enum: [name, client, revenue, last_activity]
            default: name
        - in: query
          name: page
          schema: { type: integer, minimum: 1, default: 1 }
        - in: query
          name: per_page
          schema: { type: integer, minimum: 5, maximum: 200, default: 50 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/Project' }
                  meta: { $ref: '#/components/schemas/ProjectsPagination' }
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'
    post:
      tags: [Projects]
      summary: Vytvoří zakázku
      description: |
        Klient (`client_id`) musí patřit aktuálnímu supplier-ovi. Currency se
        validuje proti `currencies` skoupeným pod stejným supplier-em.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProjectInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Project' }
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/InsufficientScope'

  /api/v1/projects/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Projects]
      summary: Detail zakázky se statistikami
      description: |
        Vrátí celý záznam zakázky + odvozené metriky:
        - `invoices_count` — kolik faktur zakázka má,
        - `revenue_by_month` — obrat po měsících za posledních 24 měsíců (per currency),
        - `revenue_by_year` — obrat po letech (per currency),
        - `unpaid_summary` — nezaplaceno / po splatnosti per currency.

        Metriky zahrnují jen vystavené faktury + dobropisy (`issued`, `sent`,
        `reminded`, `paid`); zálohy a koncepty se nepočítají.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProjectDetail' }
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Projects]
      summary: Aktualizace zakázky
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ProjectInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Project' }
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Projects]
      summary: Smazání zakázky
      description: |
        Smaže zakázku, pokud nemá vystavené doklady. Pro zakázky s historií
        použij `POST /api/v1/projects/{id}/archive`.
      responses:
        '200':
          description: OK
        '409':
          description: Zakázka má vystavené doklady — použij archivaci
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/projects/{id}/archive:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Projects]
      summary: Archivuje zakázku
      description: |
        Nastaví `archived_at = NOW()`. Archivované zakázky se nezobrazují
        v defaultním výpisu (lze je dohledat přes filtr).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Project' }

  /api/v1/clients/{client_id}/projects:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: client_id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Projects]
      summary: Zakázky daného klienta (vč. archivovaných)
      description: |
        Light-weight výpis bez stránkování — vrátí všechny zakázky klienta včetně
        archivovaných (řazené active → paused → closed → archived). Užitečné pro
        plnění selectů ve frontu integrace.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/ProjectBrief' }
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/projects/stats:
    get:
      tags: [Projects]
      summary: Agregované statistiky zakázek (pro dashboard a stránku Grafy)
      description: |
        Vrací agregace pro vizualizace na stránce **Grafy** (`/stats`):

        - **top_this_year / top_prev_year** — top 12 zakázek + agregovaná položka „Ostatní"
        - **top_12m** — top 12 zakázek za rolling 12 měsíců (smart-mix tabulky v UI;
          v lednu jsou per-rok statistiky téměř prázdné, rolling 12m je robustní)
        - **totals_per_year** — sumy + počty per (year, currency)
        - **status_breakdown** — counts per status (active/paused/closed)
        - **is_vat_payer** — určuje, který sloupec se použil pro agregaci obratu
          (plátce = `total_without_vat`, neplátce = `total_with_vat`)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ProjectStats' }

  /api/v1/invoices/{id}/work-report:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        description: ID faktury
        schema: { type: integer, format: int64 }
    get:
      tags: [WorkReports]
      summary: Výkaz víceprací k faktuře
      description: |
        Vrátí výkaz včetně všech `items` (úkony s hodinami a sazbou) nebo
        `null` pokud k faktuře žádný neexistuje.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/WorkReport'
                  - { type: "null" }
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [WorkReports]
      summary: Vytvoří nebo aktualizuje výkaz
      description: |
        Idempotentní upsert (jedna fakturace = max 1 výkaz). Výkaz lze měnit
        pouze pokud faktura má status `draft`. Admin může vynutit edit
        pomocí query parametru `?force=1` (bez ohledu na status).

        Suma `total_hours` a `total_amount` se přepočítá ze `items` na serveru.

        **Pozor:** výkaz víceprací je samostatná entita — ukládá se jen jako
        příloha (2. strana PDF) a **NEmění položky ani částku faktury**. Pokud chceš
        práci i vyúčtovat, přidej odpovídající položku faktury zvlášť přes
        `PUT /api/v1/invoices/{id}` (úprava `items`). Obojí lze dělat jen u draftu.
      parameters:
        - in: query
          name: force
          description: Admin-only override editace u nedraftových faktur
          schema: { type: boolean }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/WorkReportInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WorkReport' }
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '409':
          description: Faktura není draft (a klient není admin s ?force=1)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
    delete:
      tags: [WorkReports]
      summary: Smaže výkaz
      responses:
        '200':
          description: OK
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/invoices/{id}/request-approval:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [WorkReports]
      summary: Pošle výkaz zákazníkovi ke schválení
      description: |
        Pre-conditions:
        - faktura je `draft` a má linknutý work-report,
        - projekt má `requires_work_report_approval = true`.

        Co se stane:
        - vygeneruje se 32-bytový `approval_token`, který dostane zákazník
          v emailu (link `/approval/{token}`),
        - faktura přejde do `approval_status = "requested"` (status zůstává `draft`),
        - alokuje se variabilní symbol a zafixují snapshoty supplier/client/bank
          (faktura se po schválení vystaví bez dalších změn),
        - zákazník dostane email `invoice_approval` s **PDF výkazu** v příloze
          (ne fakturu — tu uvidí až po vystavení),
        - příjemci: kontakty klienta s účelem `approvals` (viz `email_contacts`);
          bez nich `project.billing_emails`, fallback `client.main_email`,
        - audit log: `invoice.approval_requested`.

        Po rozhodnutí zákazníka (přes veřejný link) můžeš stav zjistit
        v `GET /api/v1/invoices/{id}` (pole `approval_*`). Pokud schválí,
        může proběhnout auto-issue + auto-send dle nastavení supplier-a.
      responses:
        '200':
          description: OK — email odeslán
          content:
            application/json:
              schema:
                type: object
                properties:
                  approval_token: { type: string, example: "a1b2…" }
                  approval_status: { type: string, example: requested }
                  recipients:
                    type: array
                    items: { type: string, format: email }
        '409':
          description: Faktura není draft / nemá výkaz / projekt nevyžaduje schválení
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/approval-status:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    put:
      tags: [WorkReports]
      summary: Ruční update schvalovacího stavu (admin override)
      description: |
        Použij když potřebuješ obejít veřejný link — např. zákazník schválil
        telefonicky a chceš to v systému zaznamenat. Audit log loguje, kdo
        rozhodnutí vložil ručně.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [approval_status]
              properties:
                approval_status:
                  type: string
                  enum: [none, requested, approved, rejected]
                approval_decided_by_email:
                  type: string
                  format: email
                  description: Kdo schválil (pro audit)
                approval_rejection_reason:
                  type: string
                  description: Důvod (pokud `rejected`)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }

  /api/v1/invoices/{id}/pdfs:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceComms]
      summary: Archiv vystavených/odeslaných verzí PDF
      description: |
        Pokaždé, když se faktura vystaví, znovu-vystaví nebo pošle e-mailem,
        se PDF archivuje. Archiv slouží jako důkaz, co přesně zákazník dostal —
        i když se snapshot na faktuře později změnil (přes admin override).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/PdfArchiveEntry' }

  /api/v1/invoices/{id}/pdfs/{archiveId}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
      - in: path
        name: archiveId
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceComms]
      summary: Stažení archivované verze PDF
      responses:
        '200':
          description: PDF stream
          content:
            application/pdf:
              schema: { type: string, format: binary }
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/invoices/{id}/recipients:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceComms]
      summary: Vyřešení e-mailoví příjemci pro odeslání
      description: |
        Vrátí příjemce, na které by se daný typ zprávy odeslal (#86) — jednotný
        `RecipientResolver`: aktivní kontakty klienta s daným účelem
        (`email_contacts`, u upomínek fallback na účel `documents`); bez kontaktů
        dosavadní logika (`main_email` + `project.billing_emails`; u schvalování
        e-maily zakázky NAHRAZUJÍ hlavní e-mail). Kombinaci s e-maily zakázky
        řídí `project.billing_emails_mode`. Read-only — nic neodesílá; hodí se
        pro náhled příjemců před voláním `/send`.
      parameters:
        - in: query
          name: type
          schema: { type: string, enum: [documents, reminders, approvals], default: documents }
          description: Typ zprávy (doklad / upomínka / žádost o schválení výkazu).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  type: { type: string, enum: [documents, reminders, approvals] }
                  to:  { type: array, items: { type: string, format: email } }
                  cc:  { type: array, items: { type: string, format: email } }
                  bcc: { type: array, items: { type: string, format: email } }
                  resolved:
                    type: array
                    description: Provenance každého příjemce (zdroj + účel + popisek).
                    items:
                      type: object
                      properties:
                        email: { type: string, format: email }
                        recipient: { type: string, enum: [to, cc, bcc] }
                        source: { type: string, enum: [contact, project, main_email] }
                        usage: { type: string, nullable: true, enum: [communication, documents, reminders, approvals, null] }
                        label: { type: string, nullable: true }
        '404': { description: Faktura nenalezena }

  /api/v1/invoices/{id}/send:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceComms]
      summary: Pošle fakturu emailem klientovi
      description: |
        Vyrenderuje aktuální PDF, archivuje ho a odešle přes SMTP. Faktura musí
        být `issued` nebo navazující stav. Předmět + tělo z šablony `invoice_send`
        (per locale, per supplier override v Email šablonách).

        Body je volitelné — když chybí, příjemce vyřeší jednotný resolver (#86):
        kontakty klienta s účelem `documents` (vč. cc/bcc rolí); bez kontaktů
        `client.main_email` + `project.billing_emails`. Explicitní `to` v body
        je autoritativní (resolver se přeskočí). Response obsahuje
        `resolved_recipients` s provenancí každého příjemce.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                to:
                  type: array
                  items: { type: string, format: email }
                cc:
                  type: array
                  items: { type: string, format: email }
                bcc:
                  type: array
                  items: { type: string, format: email }
                subject:
                  type: string
                  description: Override předmětu emailu
                body:
                  type: string
                  description: Override těla (HTML); proměnné z šablony se nesubstituují
      responses:
        '200':
          description: OK — email odeslán
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  recipients:
                    type: array
                    items: { type: string, format: email }
                  resolved_recipients:
                    type: array
                    description: "Provenance příjemců (#86) — prázdné při explicitním `to` v body."
                    items:
                      type: object
                      properties:
                        email: { type: string, format: email }
                        recipient: { type: string, enum: [to, cc, bcc] }
                        source: { type: string, enum: [contact, project, main_email] }
                        usage: { type: string, nullable: true, enum: [communication, documents, reminders, approvals, null] }
                        label: { type: string, nullable: true }
                  archived_pdf_id: { type: integer, format: int64 }
        '409':
          description: Faktura není ve stavu pro odeslání (draft)
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/reminder:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceComms]
      summary: Pošle upomínku po splatnosti
      description: |
        Šablona `invoice_reminder` (per locale, per supplier override). Klient
        nemá vliv na obsah — text je z nastavení.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  reminder_count: { type: integer, description: Kolikátá upomínka }
        '409':
          description: Faktura není po splatnosti / je již zaplacená
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/attachments:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceComms]
      summary: Seznam příloh faktury
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  attachments:
                    type: array
                    items: { $ref: '#/components/schemas/InvoiceAttachment' }
    post:
      tags: [InvoiceComms]
      summary: Nahraje přílohu
      description: |
        Multipart upload (max ~10 MB). Příloha se přidá do dalšího emailu faktury.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Nahráno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InvoiceAttachment' }
        '413':
          description: Soubor je příliš velký
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/attachments/{attId}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
      - in: path
        name: attId
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceComms]
      summary: Stažení přílohy
      responses:
        '200':
          description: Soubor (typ podle původního MIME)
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
    delete:
      tags: [InvoiceComms]
      summary: Smaže přílohu
      responses:
        '200':
          description: OK

  /api/v1/clients/lookup-ares:
    post:
      tags: [Lookup]
      summary: Načte firmu z ARES (CZ obchodní rejstřík)
      description: |
        Vrátí strukturované údaje firmy podle IČO. Vhodné jako první krok
        v „create client" flow — pošli IČO, dostaneš název, adresu, DIČ
        a flag plátce DPH, ty pak pošleš do `POST /api/v1/clients`.

        Cache 24h — opakované volání pro stejné IČO nezatíží ARES.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ic]
              properties:
                ic:
                  type: string
                  pattern: '^\d{8}$'
                  example: "27082440"
      responses:
        '200':
          description: "OK (`found: false` pokud IČO neexistuje v ARES)"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AresLookupResult' }
        '400':
          description: IČO není 8 číslic
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '503':
          description: ARES dočasně nedostupný
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/clients/lookup-vies:
    post:
      tags: [Lookup]
      summary: Validuje EU DIČ přes VIES
      description: |
        Validace DIČ obchodního partnera v EU. Vrátí `valid: true/false`
        a (pokud platné) registrované jméno a adresu. Cache 24h.

        Reverse-charge fakturace pro EU plátce by měla DIČ validovat před
        vystavením — VIES je oficiální zdroj pravdy EK.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [vat_id]
              properties:
                vat_id:
                  type: string
                  description: DIČ ve formátu countryCode + číslo (např. DE123456789)
                  example: "DE123456789"
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ViesLookupResult' }
        '400':
          description: Neplatný formát DIČ
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
        '503':
          description: VIES dočasně nedostupný
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/activity:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceOps]
      summary: Audit log faktury
      description: |
        Vrátí chronologický log všech akcí, které faktura prošla
        (created, updated, issued, sent, paid, reminded, approval_requested,
        approval_decided, cancelled, ...). Vhodné pro debugging integrace
        a zobrazení timeline v custom UI.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/ActivityLogEntry' }

  /api/v1/invoices/{id}/unmark-paid:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceOps]
      summary: Vrátí fakturu z „paid" zpět do „issued"
      description: "Reverzace omylem označené platby. Audit log: `invoice.unmark_paid`."
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }
        '409':
          description: Faktura není ve stavu `paid`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/{id}/cancel:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceOps]
      summary: Storno vystavené faktury
      description: |
        Vystavená faktura **nelze smazat** — storno vytvoří záporný protiklad
        (`invoice_type = cancellation`) se stejnou částkou, který původní fakturu
        účetně ruší. Číselná řada je zachovaná (číslo původní faktury nezmizí).
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Důvod storna (audit + volitelně do PDF)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  original:     { $ref: '#/components/schemas/Invoice' }
                  cancellation: { $ref: '#/components/schemas/Invoice' }

  /api/v1/invoices/{id}/clone:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceOps]
      summary: Vytvoří kopii faktury jako nový draft
      description: |
        Kopíruje hlavičku + položky + work-report. Nový draft má dnešní
        `issue_date`, prázdný `number`, status `draft`. Vhodné pro periodicky
        opakované faktury (předplatné, paušál).
      responses:
        '201':
          description: Vytvořen nový draft
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }

  /api/v1/invoices/{id}/issue-final:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceOps]
      summary: Vystaví finální fakturu ze zaplacené zálohy (proforma → invoice)
      description: |
        Pouze pro `invoice_type = proforma` ve stavu `paid`. Vytvoří finální
        daňový doklad navázaný na zálohu (`parent_id`). Číslo a snapshoty
        se zafixují stejně jako u běžného vystavení.
      responses:
        '201':
          description: Finální faktura vytvořena
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }
        '409':
          description: Zdrojový doklad není proforma `paid`
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/invoices/bulk-reissue:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    post:
      tags: [InvoiceOps]
      summary: Hromadné znovu-vystavení faktur (admin)
      description: |
        Pro každou fakturu v `ids` re-renderuje PDF a uloží novou verzi do
        archivu. Existující snapshoty zůstávají (jen přibyde nová verze).
        Vhodné po globální změně nastavení supplier-a (logo, adresa apod.).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ids]
              properties:
                ids:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items: { type: integer, format: int64 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  reissued_count: { type: integer }
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        invoice_id: { type: integer }
                        reason: { type: string }

  /api/v1/invoices/bulk-reminder:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    post:
      tags: [InvoiceOps]
      summary: Hromadné odeslání upomínek
      description: |
        Pošle upomínku pro všechny faktury v `ids` (nebo všechny po splatnosti
        pokud `ids` chybí). Vhodné pro cron job „každý pondělí ráno upomínky".
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                ids:
                  type: array
                  items: { type: integer, format: int64 }
                  description: Konkrétní faktury; pokud chybí, všechny po splatnosti
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  sent_count: { type: integer }
                  skipped_count: { type: integer }
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        invoice_id: { type: integer }
                        reason: { type: string }

  /api/v1/invoices/export.csv:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Reports]
      summary: CSV export faktur
      description: |
        Stejné filtry jako `GET /api/v1/invoices`, ale vrátí CSV stream
        místo JSON. Vhodné pro BI / Google Sheets / Excel import.

        Sloupce: `id`, `number`, `client`, `issue_date`, `due_date`, `taxable_date`,
        `currency`, `total_excl_vat`, `total_vat`, `total_incl_vat`, `status`, `paid_at`.
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [draft, issued, sent, reminded, paid, cancelled]
        - in: query
          name: from
          schema: { type: string, format: date }
        - in: query
          name: to
          schema: { type: string, format: date }
        - in: query
          name: client_id
          schema: { type: integer }
      responses:
        '200':
          description: CSV stream
          content:
            text/csv:
              schema: { type: string }

  /api/v1/dashboard/summary:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Reports]
      summary: Dashboard summary — KPI, top klienti, obrat, forecasty, statistiky
      description: |
        Agregovaná data pro Dashboard i stránku Grafy (`/stats`). Endpoint
        sjednocuje desítky agregací do jednoho dotazu (cache je řízena DB
        cache tabulkami `*_revenue_cache`).

        **Obrat (VAT-aware):**
        Pro plátce DPH (`supplier.is_vat_payer = 1`) se ve všech agregacích
        obratu používá `total_without_vat`; pro neplátce `total_with_vat`.
        Vrácený flag `is_vat_payer` to indikuje.

        **Obsah:**
        - **kpi** — obrat per měna (this/prev year + YTD loni) + počty faktur,
          klientů a zakázek per rok + ø doba úhrady + overdue,
        - **overdue / unpaid_upcoming** — tabulky faktur po/před splatnosti,
        - **top_clients_ytd / top_clients_prev_year** — top 12 klientů YTD a loni,
        - **top_clients_12m** — top 12 klientů za rolling 12 měsíců (smart-mix v UI),
        - **revenue_by_month** — 12 + 12 měsíců (rolling + prev_year) per měna,
        - **revenue_by_year** — všechny roky s aktivitou,
        - **rolling_12m** — plovoucí 12měsíční obrat per měna (+ 12m předtím
          pro YoY); relevantní pro DPH limit (2 mil. Kč),
        - **revenue_last_30d** — live obrat za posledních 30 dní per měna,
        - **revenue_forecast** — predikce ročního obratu (medián tří projekcí: run-rate / krátkodobý růst / trend, vč. rozpětí),
        - **cashflow_ytd** — kumulativní inkasované platby letošek vs loni,
        - **cashflow_forecast** — co přiteče z neuhrazených v 30/60/90 dnech,
        - **due_buckets** — splatné dnes / tento týden / tento měsíc,
        - **aging_report** — stáří pohledávek (current/1-30/31-60/61-90/90+),
        - **payment_days_histogram** — distribuce doby úhrady (12m),
        - **vat_breakdown_12m** — rozpad obratu per sazba DPH (jen plátce DPH),
        - **invoice_size_histogram** — distribuce velikosti faktur v CZK ekv.,
        - **active_clients_count / active_recurring_count** — counts,
        - **pending_approvals** — počet žádostí o schválení výkazu,
        - **is_vat_payer / today / year / prev_year** — kontext.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/DashboardSummary' }

  /api/v1/bank-statements:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Bank]
      summary: Seznam nahraných bankovních výpisů
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/BankStatement' }

  /api/v1/bank-statements/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Bank]
      summary: Detail výpisu vč. všech transakcí
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BankStatementDetail' }
    delete:
      tags: [Bank]
      summary: Smaže výpis vč. transakcí a payment_matches (admin only)
      description: |
        Cascade delete přes FK constraints — smaže transakce výpisu i jejich
        payment_matches záznamy. Status zaplacených faktur se NEzmění (případný
        rollback paid→issued je doménou uživatele přes /unmatch endpoint).

        Vyžaduje roli `admin`. Účetní (`accountant`) může nahrávat a párovat,
        ale ne mazat (forensic integrity uzávěrek DPH/KH).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted: { type: boolean, example: true }
        '403': { description: "Forbidden — role není admin" }
        '404': { description: "Výpis nenalezen nebo nepatří current supplier" }

  /api/v1/bank-statements/{id}/download:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Bank]
      summary: Stáhne originální obsah GPC souboru
      description: |
        Vrací bajt-přesný originál uploadovaného GPC souboru (uložený od v4.1.0
        v `bank_statements.file_content`). Pro výpisy importované před v4.1.0
        není soubor k dispozici → 410 Gone.

        Klient si může před voláním zkontrolovat pole `has_file` v `BankStatement`.
      responses:
        '200':
          description: OK — GPC bajty
          headers:
            Content-Disposition:
              schema: { type: string }
              description: 'attachment; filename="<original-filename>"'
          content:
            text/plain:
              schema: { type: string, format: binary }
        '404': { description: "Výpis nenalezen" }
        '410': { description: "Soubor není k dispozici (importováno před v4.1.0)" }

  /api/v1/bank-transactions/{id}/match:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Bank]
      summary: Ručně přiřadí transakci k faktuře (a označí ji paid)
      description: |
        Použij když automatický matching nezachytil platbu (variabilní symbol
        chybí nebo neodpovídá). Označí transakci jako `matched`, fakturu jako
        `paid` s `paid_at = transaction.posted_at`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [invoice_id]
              properties:
                invoice_id: { type: integer, format: int64 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  transaction: { $ref: '#/components/schemas/BankTransaction' }
                  invoice: { $ref: '#/components/schemas/Invoice' }

  /api/v1/bank-transactions/{id}/unmatch:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Bank]
      summary: Odpojí transakci od faktury (a vrátí fakturu z paid → issued)
      responses:
        '200':
          description: OK

  /api/v1/bank-transactions/{id}/ignore:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Bank]
      summary: Označí transakci jako „ignorovat" (nesedí na žádnou fakturu)
      responses:
        '200':
          description: OK

  /api/v1/settings/supplier:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Settings]
      summary: Detail aktuálního dodavatele
      description: |
        Vrátí kompletní data supplier-a v jehož scope token operuje — adresa,
        IČO/DIČ, bankovní účty, číslování faktur, default platební podmínky.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SupplierFull' }

  /api/v1/suppliers:
    get:
      tags: [Settings]
      summary: Seznam všech dodavatelů, ke kterým má token přístup
      description: |
        Užitečné pro klienta, který chce zjistit `id` supplier-ů (pro X-Supplier-Id
        header u tokenu bez bind, nebo pro multi-supplier reporting).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/SupplierBrief' }

  /api/v1/suppliers/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: integer }
    get:
      tags: [Settings]
      summary: Detail konkrétního dodavatele
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SupplierFull' }

  /api/v1/settings/currencies:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Settings]
      summary: Měny nakonfigurované pro aktuálního dodavatele
      description: |
        Per-supplier override hlavního codebooku — obsahuje i bank-accounts
        per currency. Mutace (POST/PUT/DELETE) jsou jen přes UI, ne přes API.
      responses:
        '200':
          description: OK

  /api/v1/settings/vat-rates:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Settings]
      summary: DPH sazby aktuálního dodavatele
      responses:
        '200':
          description: OK

  /api/v1/settings/units:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Settings]
      summary: Měrné jednotky aktuálního dodavatele
      responses:
        '200':
          description: OK

  /api/v1/settings/countries:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Settings]
      summary: Země nakonfigurované pro aktuálního dodavatele
      responses:
        '200':
          description: OK

  /api/v1/bank-statements/upload:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    post:
      tags: [Bank]
      summary: Nahraje bankovní výpis (GPC/CSV/ABO)
      description: |
        Multipart upload. Server detekuje formát z obsahu a parsuje transakce.
        Auto-matching proti otevřeným fakturám se spustí po `POST /scan`.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: GPC, CSV (FIO/KB/MB formáty) nebo ABO výpis
      responses:
        '201':
          description: Naparsováno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/BankStatement' }
        '400':
          description: Neznámý formát / poškozený soubor
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }

  /api/v1/bank-statements/scan:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    post:
      tags: [Bank]
      summary: Auto-matching transakcí proti otevřeným fakturám
      description: |
        Pro všechny `unmatched` transakce zkusí najít fakturu podle:
        - variabilního symbolu (přesný match) — preferovaná cesta,
        - částky + protiúčtu (fallback).

        Pokud najde unikátní match, transakce se označí `matched`, faktura
        `paid` s `paid_at = transaction.posted_at`. Multi-match transakce
        zůstávají nepřiřazené pro manuální rozhodnutí.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                statement_id:
                  type: integer
                  format: int64
                  description: Pokud chybí, scan-uje všechny unmatched napříč všemi výpisy
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  matched_count: { type: integer }
                  multi_match_count:
                    type: integer
                    description: Transakce s víc kandidáty (nepárované, vyžadují ruční rozhodnutí)
                  remaining_unmatched: { type: integer }

  /api/v1/codebooks/countries:
    get:
      tags: [Codebooks]
      summary: Seznam zemí
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  countries:
                    type: array
                    items: { $ref: '#/components/schemas/Country' }

  /api/v1/codebooks/currencies:
    get:
      tags: [Codebooks]
      summary: Seznam podporovaných měn
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  currencies:
                    type: array
                    items: { $ref: '#/components/schemas/Currency' }

  /api/v1/codebooks/vat-rates:
    get:
      tags: [Codebooks]
      summary: Seznam DPH sazeb
      responses:
        '200':
          description: OK

  /api/v1/codebooks/units:
    get:
      tags: [Codebooks]
      summary: Seznam měrných jednotek
      responses:
        '200':
          description: OK

  /api/v1/codebooks/years:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Codebooks]
      summary: Distinct roky z faktur (zdroj pro year filter dropdown)
      description: |
        Vrací DISTINCT `YEAR(issue_date)` z `invoices` a `purchase_invoices`
        per aktuální supplier. Backend navíc doplňuje aktuální + minulý rok
        do `combined` setu (aby UI dropdown nikdy nebyl prázdný — fresh setup
        bez dokladů).

        Použij pro historické datové migrace (Fakturoid, iDoklad — typicky
        5-10+ let), kde hardcoded posledních N let v UI by data zakryl.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoices:
                    type: array
                    items: { type: integer }
                    example: [2026, 2025, 2024, 2023, 2018]
                    description: Distinct roky z `invoices` (vystavené)
                  purchase_invoices:
                    type: array
                    items: { type: integer }
                    example: [2026, 2025, 2024]
                    description: Distinct roky z `purchase_invoices` (přijaté)
                  combined:
                    type: array
                    items: { type: integer }
                    example: [2026, 2025, 2024, 2023, 2018]
                    description: Sjednocení obou množin + aktuální a minulý rok jako floor

  # ------------------------------------------------------------
  # Recurring invoice templates (pravidelné fakturace) — admin API
  # ------------------------------------------------------------
  /api/recurring:
    get:
      tags: [Recurring]
      summary: Seznam šablon pravidelných faktur (scoped na aktuálního dodavatele)
      parameters:
        - in: query
          name: client_id
          schema: { type: integer }
          description: Filtr na klienta
        - in: query
          name: status
          schema:
            type: string
            enum: [active, paused, expired]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/RecurringTemplate' }
    post:
      tags: [Recurring]
      summary: Vytvořit šablonu pravidelné faktury
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RecurringTemplateInput' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }

  /api/recurring/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: integer }
    get:
      tags: [Recurring]
      summary: Detail šablony
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }
        '404': { description: Nenalezena }
    put:
      tags: [Recurring]
      summary: Update šablony
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RecurringTemplateInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }
    delete:
      tags: [Recurring]
      summary: Smazat šablonu (vygenerované faktury zůstanou, jen ztratí vazbu)
      responses:
        '200':
          description: OK

  /api/recurring/{id}/invoices:
    get:
      tags: [Recurring]
      summary: Seznam faktur vygenerovaných z této šablony (poslední první)
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id: { type: integer }
                        varsymbol: { type: string, nullable: true }
                        invoice_type: { type: string }
                        status: { type: string }
                        issue_date: { type: string, format: date }
                        due_date: { type: string, format: date }
                        paid_at: { type: string, format: date, nullable: true }
                        total_with_vat: { type: number, format: float }
                        amount_to_pay: { type: number, format: float }
                        currency: { type: string }

  /api/recurring/{id}/pause:
    post:
      tags: [Recurring]
      summary: Pozastavit šablonu (cron přeskočí, dokud není resumed)
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: OK

  /api/recurring/{id}/resume:
    post:
      tags: [Recurring]
      summary: Obnovit šablonu (vyžaduje next_run_date <= end_date)
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: OK
        '409':
          description: Šablona vypršela (next_run > end_date)

  /api/recurring/{id}/run-now:
    post:
      tags: [Recurring]
      summary: Manuálně vygenerovat fakturu ze šablony (pro testování)
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                issue_date:
                  type: string
                  format: date
                  description: "Volitelný override; default = next_run_date šablony"
      responses:
        '201':
          description: Faktura vygenerována
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoice_id: { type: integer }
                  varsymbol: { type: string, nullable: true }
                  issued:    { type: boolean }
                  sent_to:   { type: array, items: { type: string } }
                  new_next_run_date: { type: string, format: date, nullable: true }
                  template_status:   { type: string, enum: [active, paused, expired] }

  # ============================================================================
  # Purchase invoices (přijaté faktury) — fáze 1 integrace forku
  # ============================================================================
  /api/v1/purchase-invoices:
    get:
      tags: [Purchase invoices]
      summary: Seznam přijatých faktur seskupený po měsících
      description: |
        Vrací přijaté faktury aktuálního tenant (supplier_id), seskupené podle
        `COALESCE(tax_date, issue_date)` per měsíc. Stejný response tvar jako
        `/invoices` ale s `vendor_*` poli místo `client_*`.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - in: query
          name: filter[status]
          schema: { type: string, enum: [draft, received, booked, paid, cancelled] }
        - in: query
          name: filter[document_kind]
          schema: { type: string, enum: [invoice, receipt, credit_note, advance] }
        - in: query
          name: filter[vendor_id]
          schema: { type: integer }
        - in: query
          name: filter[year]
          schema: { type: integer }
        - in: query
          name: filter[month]
          schema: { type: integer }
        - in: query
          name: filter[unpaid_only]
          schema: { type: boolean }
        - in: query
          name: filter[overdue]
          schema: { type: boolean }
        - in: query
          name: q
          schema: { type: string }
        - in: query
          name: page
          schema: { type: integer, default: 1 }
        - in: query
          name: per_page
          schema: { type: integer, default: 50, maximum: 200 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        month: { type: string, example: "2026-05" }
                        count: { type: integer }
                        totals_per_currency:
                          type: array
                          items:
                            type: object
                            properties:
                              currency: { type: string }
                              without_vat: { type: number }
                              vat: { type: number }
                              with_vat: { type: number }
                        invoices:
                          type: array
                          items: { $ref: '#/components/schemas/PurchaseInvoiceListItem' }
                  meta:
                    type: object
                    properties:
                      total: { type: integer }
                      page: { type: integer }
                      per_page: { type: integer }
                      pages: { type: integer }
    post:
      tags: [Purchase invoices]
      summary: Vytvoří draft přijaté faktury
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PurchaseInvoicePayload' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PurchaseInvoice' }
        '400': { description: Validation error }

  /api/v1/purchase-invoices/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: integer }
    get:
      tags: [Purchase invoices]
      summary: Detail přijaté faktury
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PurchaseInvoice' }
        '404': { description: Nenalezeno }
    put:
      tags: [Purchase invoices]
      summary: Update přijaté faktury (jen draft)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - in: query
          name: force
          description: Admin override pro received status (booked/paid/cancelled neumožněno).
          schema: { type: boolean }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/PurchaseInvoicePayload' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PurchaseInvoice' }
        '409': { description: Not editable }
    delete:
      tags: [Purchase invoices]
      summary: Smaže draft (vystavené faktury použij transition cancel)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses:
        '200': { description: OK }
        '409': { description: Lze smazat jen draft }

  /api/v1/purchase-invoices/{id}/items:
    put:
      tags: [Purchase invoices]
      summary: Přepíše položky a přepočte sumy
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items: { $ref: '#/components/schemas/PurchaseInvoiceItemPayload' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PurchaseInvoice' }

  /api/v1/purchase-invoices/{id}/exchange-rate:
    post:
      tags: [Purchase invoices]
      summary: Nastaví exchange rate manuálně
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                rate: { type: number, nullable: true }
                rate_date: { type: string, format: date, nullable: true }
                source: { type: string, enum: [cnb, manual, idoklad, fakturoid], default: manual }
      responses:
        '200': { description: OK }

  /api/v1/purchase-invoices/{id}/transition:
    post:
      tags: [Purchase invoices]
      summary: Přechod stavu (state machine draft → received → booked → paid; + cancelled)
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target]
              properties:
                target: { type: string, enum: [received, booked, paid, cancelled] }
                paid_date:
                  type: string
                  format: date
                  description: Pouze pro target=paid (default = dnes)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/PurchaseInvoice' }
        '409': { description: Invalid transition }

  /api/v1/purchase-invoices/{id}/pdf:
    post:
      tags: [Purchase invoices]
      summary: Upload originálního PDF od dodavatele (archive + SHA-256 dedup)
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF dokument (max 20 MiB, magic bytes %PDF- validation)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  pdf_path: { type: string }
                  pdf_hash: { type: string, description: SHA-256 hex }
                  pdf_size_bytes: { type: integer }
                  pdf_original_name: { type: string }
        '409':
          description: Stejné PDF už archivováno u jiné faktury (existing_purchase_invoice_id v error.extra)
        '413': { description: File too large (> 20 MiB) }
    get:
      tags: [Purchase invoices]
      summary: Stáhne archivované PDF od dodavatele
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: integer }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200':
          description: PDF stream
          content:
            application/pdf: {}
        '404': { description: K faktuře není archivovaný PDF }

  /api/v1/purchase-invoices/{id}/our-pdf:
    get:
      tags: [Purchase invoices]
      summary: Naše PDF rekonstrukce přijaté faktury (mPDF z dat)
      description: |
        Vygeneruje naši vlastní PDF kopii přijaté faktury. Užitečné když chybí
        originál (importované jen metadata) nebo chceme vlastní layout.
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: inline, schema: { type: integer, enum: [0,1] }, description: '1 = Content-Disposition: inline (iframe preview)' }
      responses:
        '200':
          description: PDF stream
          content:
            application/pdf: {}
        '404': { description: Faktura nenalezena }

  /api/v1/purchase-invoices/{id}/isdoc:
    get:
      tags: [Purchase invoices]
      summary: ISDOC 6.0 XML export přijaté faktury
      description: |
        Role inversion — pro přijatou je `AccountingSupplierParty` = vendor,
        `AccountingCustomerParty` = náš tenant.
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200':
          description: ISDOC XML
          content:
            application/xml: {}
        '404': { description: Faktura nenalezena }

  /api/v1/purchase-invoices/{id}/pohoda:
    get:
      tags: [Purchase invoices]
      summary: Pohoda XML (dataPack s `<pur:purchase>`) export přijaté faktury
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200':
          description: Pohoda XML
          content:
            application/xml: {}
        '404': { description: Faktura nenalezena }

  /api/v1/purchase-invoices/scan-inbox:
    post:
      tags: [Purchase invoices]
      summary: Scan adresáře s PDF/ISDOC pro automatický import (admin/accountant only)
      description: |
        Rekurzivně projde adresář nastavený v `cfg.purchase_invoice.inbox_dir`,
        deduplikuje přes SHA-256 (vůči `purchase_invoices.pdf_hash`). Z PDF s embedded
        ISDOC rozpozná data a automaticky vytvoří draft přijaté faktury. Pure PDF bez
        ISDOC se přeskakují — pro AI extrakci použij endpoint
        `POST /api/integrations/anthropic/extract` (BYOK).
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                dry_run:
                  type: boolean
                  description: True = jen vrátit seznam nálezů bez zápisu
                  default: false
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/InboxScanResult' }
        '403': { description: Pouze admin/accountant }

  /api/v1/reports/dphdp3/preview:
    get:
      tags: [Reports]
      summary: Náhled DPH přiznání (DPHDP3) pro období
      description: |
        ⚠️ XML vygenerovaný buildem je POUZE POMŮCKA. Před odesláním na EPO portál
        MFČR vždy ověř s účetní.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year,   required: true, schema: { type: integer, minimum: 2020, maximum: 2050 } }
        - { in: query, name: month,  schema: { type: integer, minimum: 1, maximum: 12 }, description: 'Pro měsíční periodu' }
        - { in: query, name: quarter, schema: { type: integer, minimum: 1, maximum: 4 }, description: 'Pro kvartální periodu' }
      responses:
        '200':
          description: JSON summary (lines 1-29 + 40+, totals, deadline)
          content:
            application/json:
              schema: { type: object }
        '403': { description: Pouze admin/accountant }

  /api/v1/reports/dphdp3:
    get:
      tags: [Reports]
      summary: Stáhne XML DPH přiznání (DPHDP3 verzePis 03.01) pro EPO portál
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, required: true, schema: { type: integer } }
        - { in: query, name: month, schema: { type: integer } }
        - { in: query, name: quarter, schema: { type: integer } }
      responses:
        '200':
          description: XML download
          content:
            application/xml: {}
        '403': { description: Pouze admin/accountant }

  /api/v1/reports/dphkh1/preview:
    get:
      tags: [Reports]
      summary: Náhled Kontrolního hlášení (DPHKH1) — vždy měsíční
      description: |
        Kontrolní hlášení se podává **vždy měsíčně**, i pro kvartální plátce DPH.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year,  required: true, schema: { type: integer } }
        - { in: query, name: month, required: true, schema: { type: integer, minimum: 1, maximum: 12 } }
      responses:
        '200': { description: Summary s counts sekcí A.1/A.4/A.5/B.1/B.2/B.3 }
        '403': { description: Pouze admin/accountant }

  /api/v1/reports/dphkh1:
    get:
      tags: [Reports]
      summary: Stáhne XML Kontrolního hlášení (DPHKH1 verzePis 03.01)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year,  required: true, schema: { type: integer } }
        - { in: query, name: month, required: true, schema: { type: integer } }
      responses:
        '200':
          description: XML download
          content:
            application/xml: {}

  /api/v1/reports/dphshv/preview:
    get:
      tags: [Reports]
      summary: Náhled Souhrnného hlášení (EU dodání) — měsíční
      description: |
        Souhrnné hlášení podávají **plátci DPH i identifikované osoby** s alespoň
        1 EU dodávkou v měsíci. Mapování VAT kódů: 20→0 (zboží), 22→2 (služby).
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year,  required: true, schema: { type: integer } }
        - { in: query, name: month, required: true, schema: { type: integer } }
      responses:
        '200': { description: Summary s řádky (country, vat_id, sh_type, amount, count) }

  /api/v1/reports/dphshv:
    get:
      tags: [Reports]
      summary: Stáhne XML Souhrnného hlášení (DPHSHV verzePis 06.01)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year,  required: true, schema: { type: integer } }
        - { in: query, name: month, required: true, schema: { type: integer } }
      responses:
        '200':
          description: XML download
          content:
            application/xml: {}

  /api/v1/reports/income-tax/preview:
    get:
      tags: [Reports]
      summary: Náhled daňového přiznání (DPFO/DPPO) — roční, MVP foundation
      description: |
        ⚠️ **MVP only** — výkaz NENÍ kompletní. Generuje XML kostru s identifikací
        poplatníka a orientačními čísly (tržby/náklady z faktur). Účetní data
        (odpisy, mzdy, soc/zdrav, zálohy, slevy) MyInvoice neeviduje — doplnit
        v účetním software nebo ve spolupráci s poradcem.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, schema: { type: integer }, description: 'Default = předchozí rok' }
        - { in: query, name: type, required: true, schema: { type: string, enum: [fo, po] } }
      responses:
        '200': { description: Orientational summary }

  /api/v1/reports/income-tax:
    get:
      tags: [Reports]
      summary: Stáhne XML daňového přiznání DPFO (DPFDP5) nebo DPPO (DPPDP9)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, schema: { type: integer } }
        - { in: query, name: type, required: true, schema: { type: string, enum: [fo, po] } }
      responses:
        '200':
          description: XML download (kostra)
          content:
            application/xml: {}

  # ── Tax submissions archive ──────────────────────────────────────────────
  /api/v1/reports/submissions:
    get:
      tags: [Reports]
      summary: Historie všech generovaných EPO XML výkazů
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200':
          description: List archived submissions
          content:
            application/json:
              schema: { type: array, items: { type: object } }

  /api/v1/reports/submissions/{id}:
    get:
      tags: [Reports]
      summary: Detail záznamu (vč. XML)
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: Submission detail }
        '404': { description: Not found }
    delete:
      tags: [Reports]
      summary: Smaže archivovaný záznam (jen admin)
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: Deleted }
        '403': { description: Pouze admin }

  /api/v1/reports/submissions/{id}/xml:
    get:
      tags: [Reports]
      summary: Stáhne archivované XML
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200':
          description: XML download
          content:
            application/xml: {}

  # ── Codebooks: cnb-rate + expense-categories + vat-classifications ──────
  /api/v1/codebooks/cnb-rate:
    get:
      tags: [Codebooks]
      summary: ČNB exchange rate pro currency + datum
      parameters:
        - { in: query, name: currency, required: true, schema: { type: string, example: "EUR" } }
        - { in: query, name: date, required: true, schema: { type: string, format: date } }
      responses:
        '200': { description: 'rate + rate_date + fallback_used + source' }

  /api/v1/expense-categories:
    get:
      tags: [Codebooks]
      summary: Seznam kategorií nákladů
      description: Per-tenant číselník pro rozpad nákladů přijatých faktur (CRM/dashboard).
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: include_archived, schema: { type: boolean, default: false }, description: Zahrnout archivované }
      responses:
        '200':
          description: Seznam kategorií
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ExpenseCategory' }
    post:
      tags: [Codebooks]
      summary: Vytvoří kategorii (admin)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, label]
              properties:
                code: { type: string, maxLength: 20, pattern: '^[a-z0-9_-]+$' }
                label: { type: string, maxLength: 100 }
                fixed_or_var: { type: string, enum: [fixed, variable] }
                display_order: { type: integer }
      responses:
        '201': { description: Created }

  /api/v1/expense-categories/{id}:
    put:
      tags: [Codebooks]
      summary: Update kategorie nákladů
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: OK }
    delete:
      tags: [Codebooks]
      summary: Smaže (nebo archivuje pokud má reference) kategorii
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: Deleted/archived }

  /api/v1/revenue-categories:
    get:
      tags: [Codebooks]
      summary: Seznam kategorií tržeb
      description: Per-tenant číselník pro rozpad tržeb vydaných faktur (CRM/dashboard/Grafy).
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: include_archived, schema: { type: boolean, default: false }, description: Zahrnout archivované }
      responses:
        '200':
          description: Seznam kategorií
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/RevenueCategory' }
    post:
      tags: [Codebooks]
      summary: Vytvoří kategorii tržby (admin)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, label]
              properties:
                code: { type: string, maxLength: 20, pattern: '^[a-z0-9_-]+$' }
                label: { type: string, maxLength: 100 }
                display_order: { type: integer }
      responses:
        '201': { description: Created }

  /api/v1/revenue-categories/{id}:
    put:
      tags: [Codebooks]
      summary: Update kategorie tržby
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: OK }
    delete:
      tags: [Codebooks]
      summary: Smaže (nebo archivuje pokud má reference) kategorii tržby
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: Deleted/archived }

  /api/v1/vat-classifications:
    get:
      tags: [Codebooks]
      summary: VAT klasifikační kódy (DPHDP3 / KH / SH)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: direction, schema: { type: string, enum: [sale, purchase] } }
        - { in: query, name: include_archived, schema: { type: boolean } }
      responses:
        '200':
          description: List kódů (globální seed + tenant overrides)
    post:
      tags: [Codebooks]
      summary: Vytvoří custom VAT kód (per tenant)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses:
        '201': { description: Created }

  /api/v1/vat-classifications/{id}:
    put:
      tags: [Codebooks]
      summary: Update VAT kódu (jen tenant rows, globální seed read-only)
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: OK }
        '403': { description: Globální seed je read-only }
    delete:
      tags: [Codebooks]
      summary: Smaže (nebo archivuje pokud má reference) VAT kód
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: Deleted/archived }

  # ── Roční daňové konstanty (globální číselník) ──────────────────────────
  /api/v1/codebooks/tax-constants:
    get:
      tags: [Codebooks]
      summary: Roční daňové konstanty (sazby, limity, paušály) za roky
      description: |
        GLOBÁLNÍ číselník (ne per-supplier) — efektivní hodnoty per rok s příznakem
        `is_override` (přepis výchozích konstant z migrace) vs. výchozí seed.
        Slouží daňovému optimalizátoru a EPO výkazům (DPFO/DPPO).
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  years:
                    type: array
                    items: { $ref: '#/components/schemas/TaxConstantsYear' }

  /api/v1/codebooks/tax-constants/{year}:
    parameters:
      - in: path
        name: year
        required: true
        schema: { type: integer, minimum: 2018, maximum: 2100 }
    put:
      tags: [Codebooks]
      summary: Uloží override daňových konstant pro rok (jen admin)
      description: |
        Přepíše výchozí konstanty pro daný rok. Tělo lze poslat buď jako plochý
        objekt, nebo zabalené v `{ "data": { ... } }`. Skalární klíče jsou povinné
        (sazby, limity), vnořené (`pausal_annual`, `band_ceilings`, `expense_caps`,
        `child_credits`) musí být objekty/pole.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TaxConstantsInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  year:        { type: integer }
                  is_override: { type: boolean, example: true }
                  data:        { type: object }
        '403': { description: Pouze admin }
        '422': { description: Neplatný rok / validace selhala }
    delete:
      tags: [Codebooks]
      summary: Reset roku na výchozí konstanty (smaže override, jen admin)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  year:        { type: integer }
                  is_override: { type: boolean, example: false }
                  data:        { type: object }
        '403': { description: Pouze admin }

  # ── Purchase invoices — chybějící endpointy ──────────────────────────────
  /api/v1/purchase-invoices/export:
    get:
      tags: [Purchase invoices]
      summary: Hromadný export přijatých faktur za měsíc nebo čtvrtletí (PDF ZIP / ISDOC ZIP / Pohoda dataPack)
      description: |
        `pdf-zip`: ZIP s jedním PDF na fakturu. Preferuje archivovaný **originál**
        od dodavatele (`Prijata-{vs}-{vendor}.pdf`); pokud originál chybí, doplní
        **naši rekonstrukci** z dat faktury (`Prijata-{vs}-{vendor}-rekonstrukce.pdf`).
        Počet rekonstrukcí je v hlavičce `X-Export-Reconstructed`.

        `isdoc` / `pohoda`: generují XML z dat faktury (PDF nepotřebují).

        Období lze zadat legacy parametrem `month=YYYY-MM`, nebo explicitně
        `period=monthly&year=YYYY&month=M` / `period=quarterly&year=YYYY&quarter=1..4`.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: month, schema: { oneOf: [{ type: string, pattern: '^\d{4}-\d{2}$' }, { type: integer, minimum: 1, maximum: 12 }] } }
        - { in: query, name: period, schema: { type: string, enum: [monthly, quarterly] } }
        - { in: query, name: year, schema: { type: integer, minimum: 1900, maximum: 2100 } }
        - { in: query, name: quarter, schema: { type: integer, minimum: 1, maximum: 4 } }
        - { in: query, name: format, required: true, schema: { type: string, enum: [pdf-zip, isdoc, pohoda] } }
        - { in: query, name: date_by, schema: { type: string, enum: [tax, issue, received] } }
      responses:
        '200':
          description: ZIP nebo XML stream
          headers:
            X-Export-Warnings:
              schema: { type: string }
              description: Per-invoice warnings (poškozený originál, selhání rekonstrukce)
            X-Export-Reconstructed:
              schema: { type: integer }
              description: Počet faktur, u kterých se místo originálu vložila naše rekonstrukce (jen pdf-zip)

  /api/v1/purchase-invoices/{id}/activity:
    get:
      tags: [Purchase invoices]
      summary: Activity log pro přijatou fakturu
      parameters:
        - { in: path, name: id, required: true, schema: { type: integer } }
        - $ref: '#/components/parameters/SupplierIdHeader'
      responses:
        '200': { description: List events }

  # ── CRM dashboard endpoints ──────────────────────────────────────────────
  /api/v1/crm/overview:
    get:
      tags: [CRM]
      summary: KPI overview (current month + last + YTD + trailing 12m + prev year per currency)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses: { '200': { description: KPI } }

  /api/v1/crm/monthly:
    get:
      tags: [CRM]
      summary: Monthly history (revenue/costs/profit per měsíc)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12, minimum: 1, maximum: 36 } }
        - { in: query, name: currency, schema: { type: string } }
      responses: { '200': { description: Monthly rows } }

  /api/v1/crm/top-clients:
    get:
      tags: [CRM]
      summary: Top klienti podle revenue
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
        - { in: query, name: limit, schema: { type: integer, default: 10 } }
        - { in: query, name: currency, schema: { type: string } }
      responses: { '200': { description: Top klienti } }

  /api/v1/crm/top-vendors:
    get:
      tags: [CRM]
      summary: Top dodavatelé podle costs
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
        - { in: query, name: limit, schema: { type: integer, default: 10 } }
      responses: { '200': { description: Top dodavatelé } }

  /api/v1/crm/aging-receivables:
    get:
      tags: [CRM]
      summary: Aging buckets pohledávek
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses: { '200': { description: Buckets not_due / overdue_30 / 60 / 90 / 90+ } }

  /api/v1/crm/aging-payables:
    get:
      tags: [CRM]
      summary: Aging buckets závazků
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses: { '200': { description: Buckets } }

  /api/v1/crm/dso:
    get:
      tags: [CRM]
      summary: Days Sales Outstanding (průměrná doba inkasa)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
      responses: { '200': { description: 'avg_days + sample_size' } }

  /api/v1/crm/payment-punctuality:
    get:
      tags: [CRM]
      summary: "% faktur zaplacených včas"
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
      responses: { '200': { description: 'on_time_pct + counts' } }

  /api/v1/crm/concentration:
    get:
      tags: [CRM]
      summary: Riziko koncentrace (Top 1/3/5 klient share + Pareto count)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
        - { in: query, name: currency, schema: { type: string } }
      responses: { '200': { description: 'top1_share + pareto_80_count + risk_level' } }

  /api/v1/crm/expense-breakdown:
    get:
      tags: [CRM]
      summary: Náklady per kategorie
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
      responses: { '200': { description: Per-category totals } }

  /api/v1/crm/churn-risk:
    get:
      tags: [CRM]
      summary: Klienti dlouho bez objednávky
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: days, schema: { type: integer, default: 60 } }
        - { in: query, name: limit, schema: { type: integer, default: 20 } }
      responses: { '200': { description: Churn risk klienti } }

  /api/v1/crm/action-items:
    get:
      tags: [CRM]
      summary: Daily TODO (overdue, bank unmatched, recurring due, payables, purchase drafts, tax/SHV deadlines, churn)
      description: |
        Vrací položky `items[]` (typ `type`: `overdue_invoices`, `bank_unmatched`,
        `recurring_due`, `overdue_payables`, `purchase_drafts`, `tax_deadline`,
        `shv_deadline`, `churn_risk`), každá s `severity`, `title`, `hint`, `link`
        a volitelně `count`/`days`. Plus `total` a `dismissed_count`.
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses: { '200': { description: 'items + total + dismissed_count' } }

  /api/v1/crm/cash-flow-forecast:
    get:
      tags: [CRM]
      summary: Cash flow prognóza N týdnů dopředu
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: weeks, schema: { type: integer, default: 4, minimum: 1, maximum: 12 } }
        - { in: query, name: currency, schema: { type: string, default: CZK } }
      responses: { '200': { description: 'weeks[] + total_in/out/net' } }

  /api/v1/crm/late-risk:
    get:
      tags: [CRM]
      summary: Late payment risk score per klient
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: limit, schema: { type: integer, default: 10 } }
      responses: { '200': { description: Top risky klienti } }

  /api/v1/crm/reminder-effectiveness:
    get:
      tags: [CRM]
      summary: Funnel zaplaceno po N. upomínce
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
      responses: { '200': { description: 'no_reminder + after_first/_second/_third_plus + never_paid' } }

  /api/v1/crm/payment-time-histogram:
    get:
      tags: [CRM]
      summary: Distribuce doby inkasa (buckets 0-7, 8-14, 15-30, 31-60, 61+)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
      responses: { '200': { description: 'buckets[] + median + p90' } }

  /api/v1/crm/recompute:
    post:
      tags: [CRM]
      summary: Manuální přepočet pre-aggregated stats (admin)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses:
        '200': { description: 'ok + elapsed_ms' }
        '403': { description: Pouze admin }

  # ─────────────────────────────── Dokumenty ───────────────────────────────
  /api/v1/documents:
    get:
      tags: [Documents]
      summary: Seznam dokumentů a podsložek v dané složce
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: folder_id, in: query, schema: { type: integer, nullable: true }, description: 'NULL/prázdné = kořen' }
        - { name: doc_type, in: query, schema: { type: string, enum: [pdf, docx, xlsx, xml, zfo, p7s, zip, image, other] } }
      responses:
        '200':
          description: Výpis složky
          content:
            application/json:
              schema:
                type: object
                properties:
                  breadcrumb: { type: array, items: { $ref: '#/components/schemas/DocumentBreadcrumb' } }
                  folders: { type: array, items: { $ref: '#/components/schemas/DocumentFolder' } }
                  documents: { type: array, items: { $ref: '#/components/schemas/Document' } }
                  max_file_bytes: { type: integer }
    post:
      tags: [Documents]
      summary: Nahrání souborů (multipart). ZFO se automaticky rozbalí; ZIP dle zip_mode.
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                'file[]': { type: array, items: { type: string, format: binary } }
                folder_id: { type: integer, nullable: true }
                zip_mode: { type: string, enum: [explode, keep], default: keep }
                'relpaths[]': { type: array, items: { type: string }, description: 'Relativní cesty složek pro upload adresáře' }
      responses:
        '200':
          description: Souhrn nahrání
          content:
            application/json:
              schema:
                type: object
                properties:
                  created: { type: integer }
                  skipped: { type: array, items: { type: object } }
                  errors: { type: array, items: { type: object } }
  /api/v1/documents/search:
    get:
      tags: [Documents]
      summary: Fulltextové vyhledávání v názvech, popisech a obsahu (MariaDB FULLTEXT)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: q, in: query, required: true, schema: { type: string, minLength: 2 } }
      responses:
        '200':
          description: Výsledky
          content:
            application/json:
              schema:
                type: object
                properties:
                  documents: { type: array, items: { $ref: '#/components/schemas/Document' } }
                  query: { type: string }
  /api/v1/documents/link-search:
    get:
      tags: [Documents]
      summary: Našeptávač pro párování — hledá faktury, přijaté faktury, klienty a projekty
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: q, in: query, required: true, schema: { type: string, minLength: 2 } }
        - { name: types, in: query, schema: { type: string }, description: 'CSV filtr: invoice,purchase_invoice,client,project' }
      responses:
        '200':
          description: Kandidáti pro vazbu
          content:
            application/json:
              schema:
                type: object
                properties:
                  results: { type: array, items: { $ref: '#/components/schemas/DocumentLinkCandidate' } }
                  query: { type: string }
  /api/v1/documents/{id}:
    get:
      tags: [Documents]
      summary: Detail dokumentu (vč. tagů, vazeb, příloh a ZFO metadat)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '200': { description: Dokument, content: { application/json: { schema: { $ref: '#/components/schemas/DocumentDetail' } } } }
        '404': { description: Nenalezeno }
    patch:
      tags: [Documents]
      summary: Editace metadat (název, popis, tagy)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: id, in: path, required: true, schema: { type: integer } }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string, nullable: true }
                tags: { type: array, items: { type: string } }
      responses:
        '200': { description: Aktualizovaný dokument, content: { application/json: { schema: { $ref: '#/components/schemas/DocumentDetail' } } } }
    delete:
      tags: [Documents]
      summary: Přesun do koše (soft-delete)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '200': { description: ok }
  /api/v1/documents/{id}/download:
    get:
      tags: [Documents]
      summary: Stažení souboru (Content-Disposition attachment)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '200': { description: Binární obsah souboru, content: { application/octet-stream: { schema: { type: string, format: binary } } } }
  /api/v1/documents/{id}/links:
    post:
      tags: [Documents]
      summary: Připojit dokument k entitě (faktura / přijatá faktura / klient / projekt)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: id, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [entity_type, entity_id]
              properties:
                entity_type: { type: string, enum: [client, invoice, purchase_invoice, project] }
                entity_id: { type: integer }
      responses:
        '200': { description: Aktuální vazby, content: { application/json: { schema: { type: object, properties: { links: { type: array, items: { $ref: '#/components/schemas/DocumentLink' } } } } } } }
  /api/v1/documents/by-entity/{type}/{id}:
    get:
      tags: [Documents]
      summary: Dokumenty navázané na entitu (oboustranné párování)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: type, in: path, required: true, schema: { type: string, enum: [client, invoice, purchase_invoice, project] } }
        - { name: id, in: path, required: true, schema: { type: integer } }
      responses:
        '200': { description: Navázané dokumenty, content: { application/json: { schema: { type: object, properties: { documents: { type: array, items: { $ref: '#/components/schemas/Document' } } } } } } }
  /api/v1/document-folders:
    get:
      tags: [Documents]
      summary: Strom složek (bez parametru celý plochý seznam, s parent_id přímé potomky)
      parameters:
        - { $ref: '#/components/parameters/SupplierIdHeader' }
        - { name: parent_id, in: query, schema: { type: integer, nullable: true } }
      responses:
        '200': { description: Složky, content: { application/json: { schema: { type: object, properties: { folders: { type: array, items: { $ref: '#/components/schemas/DocumentFolder' } } } } } } }
    post:
      tags: [Documents]
      summary: Vytvoření složky
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                parent_id: { type: integer, nullable: true }
      responses:
        '200': { description: ok }

  # ─────────────────────────── Pravidelné fakturace ────────────────────────
  /api/v1/recurring:
    get:
      tags: [Recurring]
      summary: Seznam šablon pravidelných fakturací
      description: |
        Stránkovaný seznam šablon aktuálního supplier-a. `meta.summary` obsahuje
        součet částek per měna + přepočet na CZK (dnešní ČNB kurz, pokud je víc měn).
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: client_id, schema: { type: integer } }
        - { in: query, name: status, schema: { type: string, enum: [active, paused, expired] } }
        - { in: query, name: sort, schema: { type: string, enum: [client, next_run, amount_czk] } }
        - { in: query, name: page, schema: { type: integer, minimum: 1, default: 1 } }
        - { in: query, name: per_page, schema: { type: integer, minimum: 5, maximum: 200, default: 50 } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: '#/components/schemas/RecurringTemplate' }
                  meta: { type: object }
    post:
      tags: [Recurring]
      summary: Vytvoří šablonu pravidelné fakturace
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RecurringTemplateInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }
        '400':
          $ref: '#/components/responses/ValidationFailed'

  /api/v1/recurring/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Recurring]
      summary: Detail šablony
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [Recurring]
      summary: Aktualizace šablony
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RecurringTemplateInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [Recurring]
      summary: Smaže šablonu
      responses:
        '200': { description: 'OK ({ deleted: true })' }
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/recurring/{id}/invoices:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Recurring]
      summary: Faktury vygenerované z této šablony
      description: Flat list (poslední první) — id, varsymbol, typ, stav, data, částky, currency.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { type: object } }

  /api/v1/recurring/{id}/pause:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Recurring]
      summary: Pozastaví šablonu (status → paused)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }

  /api/v1/recurring/{id}/resume:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Recurring]
      summary: Obnoví pozastavenou šablonu (status → active, přepočet next_run)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RecurringTemplate' }
        '409': { description: 'Šablona vypršela (next_run > end_date)' }

  /api/v1/recurring/{id}/run-now:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Recurring]
      summary: Manuální spuštění šablony (vygeneruje fakturu / koncept)
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                issue_date: { type: string, format: date, description: Vynucené datum vystavení }
                draft: { type: boolean, description: 'true = vytvořit koncept i u šablony s auto_issue' }
      responses:
        '201':
          description: 'Vygenerováno (invoice_id, varsymbol, issued, sent_to, new_next_run_date, template_status)'
        '409': { description: Šablonu nelze spustit (vypršela / chyba generování) }

  # ── Vydané faktury — párování zálohy ─────────────────────────────────────
  /api/v1/invoices/{id}/advance-candidates:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceOps]
      summary: Nespárované zálohové faktury (proforma) téhož odběratele
      description: Kandidáti pro propojení daňového dokladu se zálohou.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  candidates: { type: array, items: { type: object } }

  /api/v1/invoices/{id}/final-candidates:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [InvoiceOps]
      summary: Daňové doklady, se kterými lze tuto zálohu (proforma) spárovat
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  candidates: { type: array, items: { type: object } }

  /api/v1/invoices/{id}/link-advance:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [InvoiceOps]
      summary: Propojí daňový doklad se zálohovou fakturou
      description: Uloží `parent_invoice_id` a doplní `advance_paid_amount`, pokud na faktuře ještě žádná záloha není.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [advance_id]
              properties:
                advance_id: { type: integer, format: int64 }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }
        '409': { description: Propojení selhalo }
    delete:
      tags: [InvoiceOps]
      summary: Zruší propojení se zálohou
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Invoice' }

  # ── Přijaté faktury — párování zálohy ────────────────────────────────────
  /api/v1/purchase-invoices/{id}/advance-candidates:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Purchase invoices]
      summary: Nespárované zálohové přijaté faktury téhož dodavatele
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  candidates: { type: array, items: { type: object } }

  /api/v1/purchase-invoices/{id}/settlement-candidates:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Purchase invoices]
      summary: Vyúčtovací faktury, se kterými lze tuto zálohu spárovat (opačný směr)
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  candidates: { type: array, items: { type: object } }

  /api/v1/purchase-invoices/{id}/link-advance:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Purchase invoices]
      summary: Propojí finální přijatou fakturu se zálohovou (proti dvojímu nákladu)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [advance_id]
              properties:
                advance_id: { type: integer, format: int64 }
      responses:
        '200': { description: 'OK (aktualizovaná faktura vč. linked_advance)' }
        '409': { description: Propojení selhalo }
    delete:
      tags: [Purchase invoices]
      summary: Zruší propojení se zálohou
      responses:
        '200': { description: OK }

  /api/v1/purchase-invoices/{id}/advance-suggestion:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    delete:
      tags: [Purchase invoices]
      summary: Zamítne automatický návrh na spárování zálohy
      responses:
        '200': { description: OK }

  /api/v1/purchase-invoices/{id}/dismiss-extraction-warning:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    post:
      tags: [Purchase invoices]
      summary: Skryje varování z AI/automatické extrakce dokladu
      responses:
        '200': { description: OK }

  # ── CRM dashboard — doplňující endpointy ─────────────────────────────────
  /api/v1/crm/yearly:
    get:
      tags: [CRM]
      summary: Historie tržeb/nákladů/zisku po rocích
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: currency, schema: { type: string } }
      responses: { '200': { description: Roční řady } }

  /api/v1/crm/dpo:
    get:
      tags: [CRM]
      summary: Days Payable Outstanding (Ø doba úhrady dodavatelům)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
      responses: { '200': { description: 'avg_days + sample_size' } }

  /api/v1/crm/vendor-concentration:
    get:
      tags: [CRM]
      summary: Riziko koncentrace nákladů (Top dodavatelé share + Pareto)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
        - { in: query, name: currency, schema: { type: string } }
      responses: { '200': { description: 'top1_share + pareto_80_count + risk_level' } }

  /api/v1/crm/revenue-breakdown:
    get:
      tags: [CRM]
      summary: Tržby per kategorie tržeb
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: months, schema: { type: integer, default: 12 } }
        - { in: query, name: currency, schema: { type: string } }
      responses: { '200': { description: Per-category totals } }

  /api/v1/crm/action-items/dismiss:
    post:
      tags: [CRM]
      summary: Skryje položku action-items (den / týden / natrvalo)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [item_type, mode]
              properties:
                item_type: { type: string }
                mode: { type: string, description: 'day | week | forever | historical' }
      responses:
        '200': { description: ok }
        '400': { description: Neplatný vstup }

  /api/v1/crm/action-items/restore:
    post:
      tags: [CRM]
      summary: Vrátí (un-dismiss) skrytou položku action-items
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [item_type]
              properties:
                item_type: { type: string }
      responses:
        '200': { description: ok }

  /api/v1/crm/action-items/restore-all:
    post:
      tags: [CRM]
      summary: Vrátí všechny skryté položky action-items aktuálního uživatele
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses:
        '200': { description: 'ok + restored (počet)' }

  # ── Dashboard náklady ────────────────────────────────────────────────────
  /api/v1/dashboard/purchase-summary:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
    get:
      tags: [Reports]
      summary: Dashboard nákladů (zrcadlo summary nad přijatými fakturami)
      description: |
        Agregace pro stránku **Náklady** nad `purchase_invoices`: KPI per měna
        (this/prev year + YoY), po splatnosti/nezaplacené závazky, top dodavatelé
        (YTD/loni/rolling 12m), náklady po měsících/rocích, rolling 12m, cash-outflow,
        aging závazků, forecast, rozpad DPH na vstupu (12m), rozpad nákladů po
        kategoriích, histogramy. VAT-aware (`is_vat_payer`). Zálohy se vyřazují
        z nákladů dle cash sémantiky (zaplaceno a nespárováno).
      responses:
        '200': { description: OK }

  # ── Daňový optimalizátor ─────────────────────────────────────────────────
  /api/v1/tax/analysis:
    get:
      tags: [Tax]
      summary: Daňová analýza za rok (retrospektiva nebo projekce)
      description: |
        Uzavřený rok → `mode: retrospective` (srovnání režimů na skutečném příjmu +
        YoY). Běžící rok → `mode: forecast` (YTD příjem + projekce a sledování limitů
        DPH/paušál). Vrací `profile`, `constants`, `available_years`, `is_vat_payer`.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, schema: { type: integer }, description: 'Default = aktuální rok' }
      responses:
        '200': { description: OK }
        '400': { description: Žádný supplier scope }
        '422': { description: Neplatný rok }

  /api/v1/tax/profile:
    put:
      tags: [Tax]
      summary: Uloží daňový profil supplier-a pro rok
      description: |
        Profil pro optimalizátor — paušál/skutečné výdaje, sleva na manžela, počet
        dětí, úroky z hypotéky, penzijní/životní pojištění, dary, pásmo paušální daně.
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TaxProfileInput' }
      responses:
        '200': { description: 'OK ({ profile: {...} })' }
        '400': { description: Žádný supplier scope }
        '422': { description: Neplatný rok }

  # ── Klienti / vyhledávání — doplňující endpointy ─────────────────────────
  /api/v1/clients/{id}/vat-status:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - in: path
        name: id
        required: true
        schema: { type: integer, format: int64 }
    get:
      tags: [Lookup]
      summary: Online ověření plátcovství DPH klienta (ARES dle IČO / VIES dle DIČ)
      description: |
        Ověří a uloží `clients.is_vat_payer`. Když registr nerozhodne, vrátí dosud
        uložený příznak. ARES/VIES cache (24 h) → opakované volání nezatěžuje registry.
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  id: { type: integer, format: int64 }
                  is_vat_payer: { type: boolean }
                  source: { type: string, enum: [ares, vies, unknown] }
                  ic: { type: string, nullable: true }
                  dic: { type: string, nullable: true }
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/clients/lookup-bank:
    post:
      tags: [Lookup]
      summary: Zveřejněné bankovní účty z registru plátců DPH (CRPDPH) dle DIČ
      description: |
        Vrátí účty zveřejněné v registru plátců DPH (MFČR) + příznak nespolehlivého
        plátce. Vhodné při zakládání dodavatele (předvyplnění účtu z DIČ).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [dic]
              properties:
                dic:
                  type: string
                  description: DIČ — 8–10 číslic (s i bez prefixu CZ)
                  example: "CZ27082440"
      responses:
        '200': { description: 'OK (účty + nespolehlivý plátce)' }
        '400': { description: Neplatné DIČ }

  /api/v1/search:
    get:
      tags: [Lookup]
      summary: Globální vyhledávač (klienti/dodavatelé + vydané/přijaté faktury)
      description: Min. 2 znaky, jinak prázdné výsledky. Tenant scope dle supplier-a.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: q, required: true, schema: { type: string, minLength: 2 } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  q: { type: string }
                  clients: { type: array, items: { type: object } }
                  invoices: { type: array, items: { type: object } }
                  purchase_invoices: { type: array, items: { type: object } }

  # ── Reports — doplňující endpointy ───────────────────────────────────────
  /api/v1/reports/dphdp3/settings:
    get:
      tags: [Reports]
      summary: Nastavení relevantní pro DPH přiznání (perioda, kód FÚ, plátcovství)
      parameters: [{ $ref: '#/components/parameters/SupplierIdHeader' }]
      responses: { '200': { description: Settings } }

  /api/v1/reports/dphdp3/trend:
    get:
      tags: [Reports]
      summary: Trend daňové povinnosti DPH za více období
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, schema: { type: integer } }
      responses: { '200': { description: Časová řada } }

  /api/v1/reports/dphdp3/drafts-prediction:
    get:
      tags: [Reports]
      summary: Predikce dopadu konceptů na příští DPH přiznání
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, schema: { type: integer } }
        - { in: query, name: month, schema: { type: integer, minimum: 1, maximum: 12 } }
        - { in: query, name: quarter, schema: { type: integer, minimum: 1, maximum: 4 } }
      responses: { '200': { description: Predikce } }

  /api/v1/reports/dph-book/preview:
    get:
      tags: [Reports]
      summary: Náhled Knihy DPH (interní VAT žurnál, vždy měsíční)
      description: Interní pomůcka — NENÍ EPO podání.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, required: true, schema: { type: integer } }
        - { in: query, name: month, required: true, schema: { type: integer, minimum: 1, maximum: 12 } }
      responses: { '200': { description: Summary řádků žurnálu } }

  /api/v1/reports/dph-book:
    get:
      tags: [Reports]
      summary: Stáhne PDF Knihy DPH za měsíc
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, required: true, schema: { type: integer } }
        - { in: query, name: month, required: true, schema: { type: integer, minimum: 1, maximum: 12 } }
      responses:
        '200':
          description: PDF download
          content:
            application/pdf:
              schema: { type: string, format: binary }

  /api/v1/logbook/cars:
    get:
      tags: [Logbook]
      summary: Seznam vozidel
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: include_archived, schema: { type: boolean, default: false } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/Car' } }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Logbook]
      summary: Vytvoří vozidlo
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CarInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Car' }
        '400': { $ref: '#/components/responses/ValidationFailed' }
        '403': { $ref: '#/components/responses/InsufficientScope' }

  /api/v1/logbook/cars/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - { in: path, name: id, required: true, schema: { type: integer, format: int64 } }
    get:
      tags: [Logbook]
      summary: Detail vozidla
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Car' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Logbook]
      summary: Upraví vozidlo
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CarInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Car' }
        '400': { $ref: '#/components/responses/ValidationFailed' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [Logbook]
      summary: Smaže vozidlo (nebo archivuje, je-li použito)
      responses:
        '200': { description: OK }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/logbook/trips:
    get:
      tags: [Logbook]
      summary: Seznam jízd
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: car_id, schema: { type: integer } }
        - { in: query, name: category_id, schema: { type: integer } }
        - { in: query, name: year, schema: { type: integer } }
        - { in: query, name: month, schema: { type: integer } }
        - { in: query, name: date_from, schema: { type: string, format: date } }
        - { in: query, name: date_to, schema: { type: string, format: date } }
        - { in: query, name: q, schema: { type: string } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/Trip' } }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Logbook]
      summary: Vytvoří jízdu
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Trip' }
        '400': { $ref: '#/components/responses/ValidationFailed' }
        '403': { $ref: '#/components/responses/InsufficientScope' }

  /api/v1/logbook/trips/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - { in: path, name: id, required: true, schema: { type: integer, format: int64 } }
    get:
      tags: [Logbook]
      summary: Detail jízdy
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Trip' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Logbook]
      summary: Upraví jízdu
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/TripInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Trip' }
        '400': { $ref: '#/components/responses/ValidationFailed' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [Logbook]
      summary: Smaže jízdu
      responses:
        '200': { description: OK }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/logbook/fuelings:
    get:
      tags: [Logbook]
      summary: Seznam tankování
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: car_id, schema: { type: integer } }
        - { in: query, name: source, schema: { type: string } }
        - { in: query, name: year, schema: { type: integer } }
        - { in: query, name: month, schema: { type: integer } }
        - { in: query, name: date_from, schema: { type: string, format: date } }
        - { in: query, name: date_to, schema: { type: string, format: date } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/Fueling' } }
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [Logbook]
      summary: Vytvoří tankování (ruční)
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FuelingInput' }
      responses:
        '201':
          description: Vytvořeno
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Fueling' }
        '400': { $ref: '#/components/responses/ValidationFailed' }
        '403': { $ref: '#/components/responses/InsufficientScope' }

  /api/v1/logbook/fuelings/{id}:
    parameters:
      - $ref: '#/components/parameters/SupplierIdHeader'
      - { in: path, name: id, required: true, schema: { type: integer, format: int64 } }
    get:
      tags: [Logbook]
      summary: Detail tankování
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Fueling' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [Logbook]
      summary: Upraví tankování
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FuelingInput' }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Fueling' }
        '400': { $ref: '#/components/responses/ValidationFailed' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [Logbook]
      summary: Smaže tankování
      responses:
        '200': { description: OK }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/logbook/trip-categories:
    get:
      tags: [Logbook]
      summary: Číselník kategorií cest
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: include_archived, schema: { type: boolean, default: false } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/TripCategory' } }
        '401': { $ref: '#/components/responses/Unauthorized' }

  /api/v1/logbook/summary:
    get:
      tags: [Logbook]
      summary: Roční daňový/účetní souhrn (per vozidlo)
      description: |
        Souhrn za zvolený rok: ujeto km, rozpad služební/soukromé (poměr pro krácení),
        stav tachometru počítaný z jízd, tankování (litry, náklad, spotřeba),
        kontrola návaznosti tachometru a informativní srovnání s paušálem na dopravu.
      parameters:
        - $ref: '#/components/parameters/SupplierIdHeader'
        - { in: query, name: year, schema: { type: integer } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/LogbookSummary' }
        '401': { $ref: '#/components/responses/Unauthorized' }

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: mi_pat_
      description: Personal Access Token získaný v Nastavení → API tokeny.

  parameters:
    SupplierIdHeader:
      in: header
      name: X-Supplier-Id
      required: false
      description: |
        ID supplier-a (firmy), v jejíž kontextu má operace běžet. Vyžadováno
        jen u **multi-supplier účtů s tokenem bez bind**. Pokud je token bound
        na konkrétního supplier-a, hlavička se ignoruje.
      schema: { type: integer }

  responses:
    Unauthorized:
      description: Neplatný nebo chybějící token
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    NotFound:
      description: Zdroj nenalezen
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    ValidationFailed:
      description: Validace selhala
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    InsufficientScope:
      description: Token nemá dostatečný scope (např. `read` na write operaci)
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }
    RateLimited:
      description: Překročen rate limit
      headers:
        Retry-After:
          schema: { type: integer }
          description: Sekundy do uvolnění
      content:
        application/json:
          schema: { $ref: '#/components/schemas/Error' }

  schemas:
    RevenueCategory:
      type: object
      properties:
        id: { type: integer }
        code: { type: string, maxLength: 20 }
        label: { type: string, maxLength: 100 }
        display_order: { type: integer }
        archived: { type: boolean }
        created_at: { type: string }
        invoices_count:
          type: integer
          description: Počet vydaných faktur v této kategorii (jen v listu)
    ExpenseCategory:
      type: object
      properties:
        id: { type: integer }
        code: { type: string, maxLength: 20 }
        label: { type: string, maxLength: 100 }
        fixed_or_var: { type: string, enum: [fixed, variable] }
        display_order: { type: integer }
        archived: { type: boolean }
        created_at: { type: string }
        purchases_count:
          type: integer
          description: Počet přijatých faktur v této kategorii (jen v listu)
    TaxConstantsYear:
      type: object
      description: Efektivní daňové konstanty pro rok (výchozí seed nebo override).
      additionalProperties: true
      properties:
        year: { type: integer }
        is_override:
          type: boolean
          description: true = přepsáno administrátorem, false = výchozí konstanty
    TaxConstantsInput:
      type: object
      description: |
        Daňové konstanty pro rok. Lze poslat ploše nebo zabaleně v `{ "data": {...} }`.
        Všechny skalární klíče jsou povinné; vnořené struktury musí být objekty/pole.
      required:
        - credit_taxpayer
        - credit_spouse
        - tax_rate_low
        - tax_rate_high
        - tax_high_threshold
        - social_rate
        - health_rate
        - social_assessment_pct
        - health_assessment_pct
        - social_min_base_main
        - social_min_base_secondary
        - social_secondary_participation_threshold
        - health_min_base
        - mortgage_cap
        - pension_cap
        - vat_limit_low
        - vat_limit_high
        - vat_rate_standard
        - vat_rate_reduced
        - kh_item_threshold
        - pausal_annual
        - band_ceilings
        - expense_caps
        - child_credits
      properties:
        credit_taxpayer: { type: number, description: Sleva na poplatníka (Kč/rok) }
        credit_spouse: { type: number, description: Sleva na manžela/ku }
        tax_rate_low: { type: number, description: Nižší sazba daně z příjmu (%) }
        tax_rate_high: { type: number, description: Vyšší sazba daně z příjmu (%) }
        tax_high_threshold: { type: number, description: Práh pro vyšší sazbu }
        social_rate: { type: number }
        health_rate: { type: number }
        social_assessment_pct: { type: number }
        health_assessment_pct: { type: number }
        social_min_base_main: { type: number }
        social_min_base_secondary: { type: number }
        social_secondary_participation_threshold: { type: number, description: Rozhodná částka (zisk) pro povinnou účast na důch. pojištění u vedlejší SVČ }
        health_min_base: { type: number }
        mortgage_cap: { type: number, description: Strop odpočtu úroků z hypotéky }
        pension_cap: { type: number }
        vat_limit_low: { type: number, description: Limit obratu pro povinnou registraci DPH }
        vat_limit_high: { type: number }
        vat_rate_standard: { type: number, description: Základní sazba DPH (%) — sloupce DPH výkazů, samovyměření RC, auto-klasifikace }
        vat_rate_reduced: { type: number, description: Snížená sazba DPH (%) }
        kh_item_threshold: { type: number, description: Limit KH (Kč vč. DPH) — nad limit doklad jednotlivě (A.4/B.2), do limitu sumace (A.5/B.3) }
        pausal_annual:
          type: object
          description: Měsíční/roční částky paušální daně per pásmo
        band_ceilings:
          type: object
          description: Stropy obratu pro pásma paušální daně
        expense_caps:
          type: object
          description: Stropy výdajových paušálů per míra (40/60/80 %)
        child_credits:
          type: array
          minItems: 1
          description: Slevy na dítě (1./2./3.+ dítě)
          items: { type: number }
    TaxProfileInput:
      type: object
      properties:
        year: { type: integer, description: 'Default = aktuální rok' }
        activity_rate: { type: integer, description: 'Výdajový paušál v % (40/60/80)', default: 60 }
        use_actual_expenses: { type: boolean }
        actual_expenses: { type: number }
        flat_tax_band: { type: string, description: Pásmo paušální daně }
        is_secondary: { type: boolean, description: Vedlejší činnost }
        spouse_credit: { type: boolean }
        children_count: { type: integer }
        mortgage_interest: { type: number }
        pension_contrib: { type: number }
        life_insurance: { type: number }
        donations: { type: number }
    DocumentBreadcrumb:
      type: object
      properties:
        id: { type: integer }
        name: { type: string }
    DocumentFolder:
      type: object
      properties:
        id: { type: integer }
        parent_id: { type: integer, nullable: true }
        name: { type: string }
        created_at: { type: string }
        subfolder_count: { type: integer, description: "Naplněno jen v listingu potomků; jinak 0" }
        file_count: { type: integer, description: "Naplněno jen v listingu potomků; jinak 0" }
        total_bytes: { type: integer, description: "Součet velikostí aktivních souborů ve složce (jen v listingu potomků)" }
    Document:
      type: object
      properties:
        id: { type: integer }
        supplier_id: { type: integer }
        folder_id: { type: integer, nullable: true }
        title: { type: string }
        description: { type: string, nullable: true }
        original_name: { type: string }
        filename: { type: string, description: "Název souboru na disku ({sha8}-{safeName})" }
        sha256: { type: string }
        mime_type: { type: string }
        size_bytes: { type: integer }
        doc_type: { type: string, enum: [pdf, docx, xlsx, xml, zfo, p7s, zip, image, other] }
        source: { type: string, enum: [manual, zfo_extract, zip_extract] }
        parent_document_id: { type: integer, nullable: true }
        signature_for_id: { type: integer, nullable: true }
        text_status: { type: string, enum: [none, extracted, unsupported, failed] }
        thumb_status: { type: string, enum: [none, generated, unsupported, failed] }
        has_thumb: { type: boolean }
        uploaded_by: { type: integer, nullable: true, description: "User ID nahrávajícího" }
        created_at: { type: string }
        deleted_at: { type: string, nullable: true, description: "Soft-delete (koš); NULL = aktivní" }
    DocumentLink:
      type: object
      properties:
        entity_type: { type: string, enum: [client, invoice, purchase_invoice, project] }
        entity_id: { type: integer }
        label: { type: string }
    DocumentLinkCandidate:
      type: object
      properties:
        entity_type: { type: string, enum: [client, invoice, purchase_invoice, project] }
        entity_id: { type: integer }
        label: { type: string }
        sublabel: { type: string }
        meta: { type: string }
    DocumentDmsMessage:
      type: object
      description: Metadata datové zprávy (ZFO) z ISDS
      properties:
        dm_id: { type: string, nullable: true }
        direction: { type: string, enum: [sent, received, unknown] }
        sender_box_id: { type: string, nullable: true }
        sender_name: { type: string, nullable: true }
        recipient_box_id: { type: string, nullable: true }
        recipient_name: { type: string, nullable: true }
        annotation: { type: string, nullable: true, description: Předmět zprávy }
        delivery_time: { type: string, nullable: true }
        acceptance_time: { type: string, nullable: true }
    DocumentDetail:
      allOf:
        - { $ref: '#/components/schemas/Document' }
        - type: object
          properties:
            tags: { type: array, items: { type: string } }
            links: { type: array, items: { $ref: '#/components/schemas/DocumentLink' } }
            attachments: { type: array, items: { $ref: '#/components/schemas/Document' } }
            dms_message: { $ref: '#/components/schemas/DocumentDmsMessage' }
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string, example: validation_failed }
            message: { type: string, example: "Pole 'name' je povinné." }

    Pagination:
      type: object
      properties:
        page: { type: integer, example: 1 }
        per_page: { type: integer, example: 50 }
        total: { type: integer, example: 423 }

    ApiMe:
      type: object
      properties:
        user:
          type: object
          properties:
            id: { type: integer }
            email: { type: string, format: email }
            name: { type: string }
            role: { type: string, enum: [admin, accountant, readonly] }
        supplier:
          oneOf:
            - $ref: '#/components/schemas/SupplierBrief'
            - { type: "null" }
        auth_method:
          type: string
          enum: [bearer, session]
        token:
          oneOf:
            - type: object
              properties:
                id: { type: integer }
                name: { type: string }
                prefix: { type: string, example: "mi_pat_abcd" }
                scope: { type: string, enum: [read, read_write] }
                expires_at: { type: string, format: date-time, nullable: true }
            - { type: "null" }

    SupplierBrief:
      type: object
      properties:
        id: { type: integer }
        company_name: { type: string }
        display_name: { type: string, nullable: true }

    Country:
      type: object
      properties:
        id: { type: integer }
        code: { type: string, example: CZ }
        name_cs: { type: string }
        name_en: { type: string }
        in_eu: { type: boolean }

    Currency:
      type: object
      properties:
        id: { type: integer }
        code: { type: string, example: CZK }
        symbol: { type: string, example: "Kč" }

    Money:
      type: object
      properties:
        amount: { type: number, format: float, example: 12500.50 }
        currency: { type: string, example: CZK }

    Client:
      type: object
      description: |
        Klient = firma v roli odběratele a/nebo dodavatele (jedna řádka, flagy
        `is_customer` / `is_vendor`). Stav archivace je timestamp `archived_at`
        (NULL = aktivní), ne boolean.
      properties:
        id: { type: integer }
        supplier_id: { type: integer }
        company_name: { type: string }
        first_name: { type: string, nullable: true }
        last_name: { type: string, nullable: true }
        ic: { type: string, nullable: true, description: IČO (8 číslic) }
        dic: { type: string, nullable: true, description: "DIČ / VAT ID s country prefixem (u SK klienta = IČ DPH)" }
        tax_number: { type: string, nullable: true, description: "Národní daňové číslo bez prefixu (SK DIČ, DE/AT Steuernummer, PL NIP, HU Adószám) — issue #120" }
        street: { type: string }
        city: { type: string }
        zip: { type: string }
        country_id: { type: integer }
        country_iso2: { type: string, description: "Denormalizovaný ISO2 kód země (alias z countries)" }
        main_email: { type: string, format: email, description: "Hlavní email klienta — povinný. Bez e-mailových kontaktů (email_contacts) na něj chodí všechny zprávy; s kontakty slouží jako fallback a pro běžnou komunikaci." }
        phone: { type: string, nullable: true }
        language: { type: string, enum: [cs, en], description: "Jazyk faktur a emailů. Default cs." }
        currency_default_id: { type: integer, nullable: true, description: "Výchozí měna pro faktury klienta. Default = supplier.default_currency_id." }
        currency_default: { type: string, description: "Denormalizovaný kód výchozí měny (alias z currencies)" }
        vat_rate_default_id: { type: integer, nullable: true, description: "Legacy výchozí sazba DPH" }
        hourly_rate: { type: number, format: float, nullable: true, description: "Default hodinová sazba (CZK / hod) pro projekty klienta" }
        reverse_charge: { type: boolean, description: "Přenesená daňová povinnost — propisuje se do nových faktur." }
        is_customer: { type: boolean, description: "Role odběratele (vystavujeme mu faktury)" }
        is_vendor: { type: boolean, description: "Role dodavatele (evidujeme přijaté faktury)" }
        is_vat_payer: { type: boolean, description: "Plátce DPH (ověřitelné přes /clients/{id}/vat-status)" }
        auto_send_reminders: { type: boolean, description: "Automatické odesílání upomínek" }
        payment_due_default: { type: integer, nullable: true, description: "Default splatnost pro nové faktury klienta" }
        payment_due_unit: { type: string, enum: [days, month], nullable: true, description: "Jednotka splatnosti (dny / konec měsíce)" }
        note: { type: string, nullable: true }
        default_expense_category_id: { type: integer, nullable: true, description: "Default kategorie nákladu pro přijaté faktury dodavatele" }
        default_revenue_category_id: { type: integer, nullable: true, description: "Default kategorie tržby pro vydané faktury odběratele" }
        invoice_number_format: { type: string, nullable: true, description: "Per-klient override číselné řady faktur" }
        proforma_number_format: { type: string, nullable: true }
        credit_note_number_format: { type: string, nullable: true }
        invoice_number_period: { type: string, enum: [year, month, none], nullable: true }
        archived_at: { type: string, format: date-time, nullable: true, description: "NULL = aktivní; vyplněno = archivováno" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    ClientEmailContact:
      type: object
      description: "E-mailový kontakt odběratele dle účelu (#86). Účely: communication (běžná komunikace), documents (faktury/dobropisy/poděkování za platbu), reminders (upomínky; bez nich fallback na documents), approvals (schvalování výkazů). Jakmile má klient aktivní kontakt s daným účelem, main_email se pro tento typ zprávy přestane automaticky přidávat (zůstává jen fallback)."
      properties:
        id: { type: integer, readOnly: true }
        email: { type: string, format: email }
        label: { type: string, nullable: true, maxLength: 80, description: 'Popisek (např. „účetní oddělení")' }
        contact_name: { type: string, nullable: true, maxLength: 120, description: "Jméno kontaktní osoby" }
        is_active: { type: boolean, default: true }
        sort_order: { type: integer, readOnly: true, description: "Pořadí — odvozeno z pořadí v zaslaném poli" }
        usages:
          type: array
          description: "Účely použití kontaktu + role příjemce."
          items:
            type: object
            required: [usage]
            properties:
              usage: { type: string, enum: [communication, documents, reminders, approvals] }
              recipient: { type: string, enum: [to, cc, bcc], default: to }

    ClientDetail:
      allOf:
        - $ref: '#/components/schemas/Client'
        - type: object
          properties:
            email_contacts:
              type: array
              items: { $ref: '#/components/schemas/ClientEmailContact' }
            projects:
              type: array
              items: { $ref: '#/components/schemas/Project' }
            invoices_count:
              type: integer
              description: Celkový počet faktur klienta.
            revenue_by_month:
              type: array
              description: "Obrat po měsících za posledních 24 měsíců (VAT-aware)."
              items:
                type: object
                properties:
                  month: { type: string, example: "2026-04" }
                  currency: { type: string }
                  total: { type: number, format: float }
            revenue_by_year:
              type: array
              description: "Obrat po letech (VAT-aware)."
              items:
                type: object
                properties:
                  year: { type: integer }
                  currency: { type: string }
                  total: { type: number, format: float }
                  count: { type: integer }
            revenue_by_project:
              type: array
              description: 'Obrat agregovaný per zakázka — pro graf "Obrat podle zakázek". Faktury bez project_id agregovány pod project_id=null.'
              items:
                type: object
                properties:
                  project_id: { type: integer, nullable: true }
                  project_name: { type: string, nullable: true }
                  currency: { type: string }
                  total: { type: number, format: float }
                  count: { type: integer }
            unpaid_summary:
              type: array
              items:
                type: object
                properties:
                  currency: { type: string }
                  unpaid_total:  { type: number, format: float }
                  unpaid_count:  { type: integer }
                  overdue_total: { type: number, format: float }
                  overdue_count: { type: integer }

    ClientInput:
      type: object
      required: [company_name, street, city, zip, main_email]
      properties:
        company_name: { type: string }
        first_name: { type: string, nullable: true }
        last_name: { type: string, nullable: true }
        ic: { type: string, nullable: true, description: "IČO — 8 číslic" }
        dic: { type: string, nullable: true, description: "DIČ / VAT ID s country prefixem (u SK = IČ DPH)" }
        tax_number: { type: string, nullable: true, description: "Národní daňové číslo bez prefixu (SK DIČ, DE/AT Steuernummer, PL NIP, HU Adószám)" }
        street: { type: string }
        city: { type: string }
        zip: { type: string }
        country_iso2: { type: string, default: CZ, description: "ISO2 kód země (resolvuje se na country_id). POZOR: vstup je `country_iso2`, ne `country_id`." }
        main_email: { type: string, format: email, description: "Hlavní email — povinný (POZOR: backend pole se jmenuje `main_email`, ne `email`)" }
        phone: { type: string, nullable: true, maxLength: 40 }
        language: { type: string, enum: [cs, en], default: cs }
        currency_default_id: { type: integer, nullable: true, description: "Výchozí měna klienta. Pokud chybí, použije se výchozí měna supplier-a." }
        currency_default: { type: string, description: "Legacy — kód měny (CZK…). Použij currency_default_id." }
        hourly_rate: { type: number, format: float, nullable: true, description: "Default hodinová sazba pro nové projekty (≥ 0)" }
        reverse_charge: { type: boolean, default: false }
        is_customer: { type: boolean, default: true, description: "Role odběratele. Aspoň jedna z is_customer/is_vendor musí zůstat true." }
        is_vendor: { type: boolean, default: false, description: "Role dodavatele." }
        is_vat_payer: { type: boolean, default: true }
        auto_send_reminders: { type: boolean, default: true }
        payment_due_default: { type: integer, minimum: 1, maximum: 365, description: "Default splatnost" }
        payment_due_unit: { type: string, enum: [days, month], nullable: true }
        note: { type: string, nullable: true }
        default_expense_category_id: { type: integer, nullable: true }
        default_revenue_category_id: { type: integer, nullable: true }
        invoice_number_format: { type: string, nullable: true, maxLength: 60 }
        proforma_number_format: { type: string, nullable: true }
        credit_note_number_format: { type: string, nullable: true }
        invoice_number_period: { type: string, enum: [year, month, none], nullable: true }
        email_contacts:
          type: array
          description: "E-mailové kontakty dle účelu (#86) — replace-all: pošli kompletní pole; klíč vynech, pokud kontakty neměníš. Max 10 kontaktů."
          items: { $ref: '#/components/schemas/ClientEmailContact' }

    Invoice:
      type: object
      properties:
        id: { type: integer }
        supplier_id: { type: integer }
        client_id: { type: integer }
        client_company_name: { type: string, description: "Denormalizováno z klienta pro pohodlí výpisů" }
        client_main_email: { type: string, format: email, nullable: true }
        project_id: { type: integer, nullable: true }
        project_name: { type: string, nullable: true }
        parent_invoice_id: { type: integer, nullable: true, description: "Pro credit_note / cancellation / final-z-proformy odkaz na původní fakturu" }
        recurring_template_id: { type: integer, nullable: true, description: "Pro faktury vygenerované ze šablony pravidelné fakturace — odkaz na šablonu (NULL pokud byla šablona smazána, viz ON DELETE SET NULL)." }
        varsymbol: { type: string, nullable: true, description: "Variabilní symbol = číslo faktury. Přiděluje se při issue (u draft je null nebo manuální override)." }
        invoice_type:
          type: string
          enum: [invoice, proforma, credit_note, cancellation, tax_document]
          description: "tax_document = daňový doklad k přijaté platbě zálohy (§ 28/2 ZDPH); DUZP = datum platby, parent_invoice_id = proforma."
        status:
          type: string
          enum: [draft, issued, sent, reminded, paid, cancelled]
        payment_status:
          type: string
          nullable: true
          enum: [unpaid, partially_paid, paid, overpaid]
          description: "Odvozený platební stav (#89) z paid_total vs. amount_to_pay (tolerance 0,05). Null pro draft/cancelled."
        payment_method:
          type: string
          enum: [bank_transfer, card, cash, other]
          default: bank_transfer
          description: "Způsob úhrady. U card/cash se v PDF/emailu nezobrazuje QR ani bankovní spojení a místo výzvy k platbě se uvede odpovídající poznámka."
        language: { type: string, enum: [cs, en], description: "Jazyk PDF/emailů" }
        reverse_charge: { type: boolean }
        prices_include_vat: { type: boolean, description: "Ceny položek zadané včetně DPH (DPH se počítá shora koeficientem). Per-doklad, migrace 0083." }
        auto_send_reminders: { type: boolean, description: "Per-faktura přepínač automatických upomínek (migrace 0088). False = cron tuto fakturu přeskočí, i když má dodavatel i klient upomínky zapnuté." }
        client_ic: { type: string, nullable: true, description: "Denormalizováno z klienta" }
        client_dic: { type: string, nullable: true }
        revenue_category_id: { type: integer, nullable: true, description: "FK do /revenue-categories — kategorie tržby pro CRM/Grafy" }
        revenue_category_label: { type: string, nullable: true, description: "Denormalizovaný label kategorie tržby" }
        revenue_category_code: { type: string, nullable: true }
        revenue_category: { type: string, nullable: true, description: "Legacy volný textový tag (nahrazeno revenue_category_id)" }
        vat_classification_code: { type: string, nullable: true, description: "Hlavičkový default kódu klasifikace plnění (DPHDP3 / KH)" }
        issue_date: { type: string, format: date }
        due_date: { type: string, format: date }
        tax_date: { type: string, format: date, nullable: true, description: "DUZP — null u proforem" }
        currency_id: { type: integer, description: "FK do /settings/currencies (per-supplier)" }
        currency: { type: string, example: CZK, description: "Denormalizovaný kód měny (= currencies.code)" }
        currency_symbol: { type: string, example: "Kč" }
        exchange_rate: { type: number, format: float, nullable: true, description: "CZK / 1 jednotka cizí měny (ČNB). NULL = neaplikováno (např. faktura v CZK)" }
        exchange_rate_date: { type: string, format: date, nullable: true }
        total_without_vat: { type: number, format: float }
        total_vat: { type: number, format: float }
        total_with_vat: { type: number, format: float }
        rounding: { type: number, format: float, description: "Zaokrouhlení (CZK)" }
        advance_paid_amount: { type: number, format: float, description: "Záloha k odečtení (u faktury vystavované po proforme)" }
        amount_to_pay: { type: number, format: float, description: "total_with_vat + rounding − advance_paid_amount" }
        paid_total: { type: number, format: float, description: "Suma evidovaných plateb (invoice_payments, #89); zbývá k úhradě = amount_to_pay − paid_total" }
        discount_percent: { type: number, format: float, description: "Procentuální sleva z celé faktury (0 = žádná). Materializuje se jako záporná položka „Sleva X %\" v `items`." }
        totals:
          type: object
          description: "Agregát pro renderování — duplikuje stejné hodnoty co výše"
          properties:
            without_vat: { type: number, format: float }
            vat: { type: number, format: float }
            with_vat: { type: number, format: float }
            rounding: { type: number, format: float }
            advance_paid_amount: { type: number, format: float }
            amount_to_pay: { type: number, format: float }
            discount_percent: { type: number, format: float, description: "Procentuální sleva z celé faktury (0 = žádná)" }
            discount_amount: { type: number, format: float, description: "Kladná peněžní hodnota slevy (= součet záporných slevových položek)" }
        vat_breakdown:
          type: array
          description: "Per-sazba rozpad DPH (vstup pro QR + PDF)"
          items:
            type: object
            properties:
              vat_rate_id: { type: integer }
              rate: { type: number, format: float, example: 21 }
              base: { type: number, format: float }
              vat:  { type: number, format: float }
              total: { type: number, format: float }
        czk_recap:
          nullable: true
          description: "CZK rekapitulace u cizoměnových faktur (null pokud měna == CZK nebo není zafixován kurz)"
          type: object
        note: { type: string, nullable: true, description: "Generická poznámka. Pro výstup viz note_above_items / note_below_items." }
        note_above_items: { type: string, nullable: true }
        note_below_items: { type: string, nullable: true }
        bank_account_number: { type: string, nullable: true, description: "Z currencies (per-měna bank účet supplier-a)" }
        bank_code: { type: string, nullable: true }
        bank_iban: { type: string, nullable: true }
        bank_bic: { type: string, nullable: true }
        approval_status:
          type: string
          nullable: true
          enum: [none, requested, approved, rejected]
        items:
          type: array
          items: { $ref: '#/components/schemas/InvoiceItem' }
        project_billing_emails:
          type: array
          items:
            type: object
            properties:
              email: { type: string, format: email }
              label: { type: string, nullable: true }
        final_invoice:
          type: object
          nullable: true
          description: "Jen u proformy — navázaný finální daňový doklad ({ id, varsymbol, status })"
        parent_invoice:
          type: object
          nullable: true
          description: "Jen pokud je parent_invoice_id ({ id, varsymbol, status, invoice_type })"
        has_advance_candidates: { type: boolean, description: "UI flag — existují nespárované zálohy k propojení" }
        has_final_candidates: { type: boolean }
        sent_at: { type: string, format: date-time, nullable: true, description: "Kdy byla naposledy odeslána emailem" }
        last_reminder_at: { type: string, format: date-time, nullable: true }
        reminder_count: { type: integer, description: "Počet odeslaných upomínek" }
        paid_at: { type: string, format: date, nullable: true }
        cancelled_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    InvoiceInput:
      type: object
      required: [client_id, items]
      description: |
        Vstup pro POST /invoices (vytvoří draft) i PUT /invoices/{id} (úprava draftu).
        Vystavená/zaplacená/storno faktura nelze upravovat — vrátí 409.
      properties:
        client_id: { type: integer, description: "Musí patřit aktuálnímu supplier-ovi" }
        project_id: { type: integer, nullable: true, description: "Pokud uvedeno, musí patřit `client_id`" }
        parent_invoice_id: { type: integer, nullable: true, description: "Jen při create (credit_note / final-z-proformy). Na UPDATE se ignoruje." }
        invoice_type:
          type: string
          enum: [invoice, proforma, credit_note, cancellation]
          default: invoice
        issue_date: { type: string, format: date, description: "Default = dnes" }
        due_date: { type: string, format: date, description: "Default = issue_date + payment_due_days (z projektu/klienta/supplier)" }
        tax_date: { type: string, format: date, nullable: true, description: "DUZP. U proform se ignoruje (uloží se null)." }
        currency_id: { type: integer, description: "FK do /settings/currencies (per-supplier). Default = výchozí měna projektu/klienta/supplier." }
        currency:
          type: string
          example: CZK
          deprecated: true
          description: "Legacy — kód měny (CZK, EUR, …) resolvuje se na `currency_id` v rámci supplier-a. Preferuj `currency_id`."
        language: { type: string, enum: [cs, en], description: "Default = language klienta (cs)" }
        reverse_charge: { type: boolean, description: "Přenesená daňová povinnost. Default = z klienta." }
        prices_include_vat: { type: boolean, default: false, description: "Ceny položek zadané včetně DPH (DPH shora koeficientem)." }
        auto_send_reminders: { type: boolean, default: true, description: "Per-faktura přepínač automatických upomínek (migrace 0088). False = cron tuto fakturu přeskočí." }
        revenue_category_id: { type: integer, nullable: true, description: "Kategorie tržby. Když chybí, na create se odvodí z defaultu projektu/klienta." }
        vat_classification_code: { type: string, nullable: true, description: "Hlavičkový default klasifikace plnění; auto-default podle sazby/RC/země, pokud prázdné." }
        varsymbol:
          type: string
          maxLength: 20
          nullable: true
          description: "Volitelný manuální override automatického číslování. U draftu lze ponechat null — číslo se přidělí při /issue."
        advance_paid_amount: { type: number, format: float, default: 0, description: "Záloha k odečtení od total_with_vat" }
        discount_percent:
          type: number
          format: float
          minimum: 0
          maximum: 100
          default: 0
          description: |
            Procentuální sleva z celé faktury (0–100). Při uložení se materializuje jako
            záporná položka „Sleva X %" rozpadlá na každou sazbu DPH zvlášť (proto se
            správně propíše do DPH rozpisu i daňových výkazů). Slevovou položku
            NEposílej ručně v `items` — generuje se z tohoto pole.
        payment_method:
          type: string
          enum: [bank_transfer, card, cash, other]
          default: bank_transfer
          description: "Způsob úhrady. Pro card/cash se v PDF/emailu nevygeneruje QR ani bankovní spojení."
        exchange_rate: { type: number, format: float, nullable: true, description: "Volitelný manuální override ČNB kurzu (CZK / 1 jednotka cizí měny)" }
        note: { type: string, nullable: true, description: "Generická poznámka (mapuje se na note_below_items pokud není uvedeno jinak)" }
        note_above_items: { type: string, nullable: true }
        note_below_items: { type: string, nullable: true }
        items:
          type: array
          items: { $ref: '#/components/schemas/InvoiceItemInput' }

    ProjectBilingEmail:
      type: object
      required: [position, email]
      properties:
        position: { type: integer, minimum: 1, maximum: 3 }
        email: { type: string, format: email }
        label: { type: string, nullable: true, example: "účetní" }
        usages:
          type: array
          nullable: true
          description: "Typy zpráv, pro které se e-mail použije (#86). NULL / prázdné / všechny tři = všechny typy (default, zpětně kompatibilní)."
          items: { type: string, enum: [documents, reminders, approvals] }

    ProjectBrief:
      type: object
      description: Zkrácená reprezentace zakázky (pro výpisy a selecty).
      properties:
        id: { type: integer, format: int64 }
        name: { type: string }
        status: { type: string, enum: [active, paused, closed] }
        currency_id: { type: integer }
        currency: { type: string, example: CZK }
        hourly_rate: { type: number, format: float, example: 1500 }
        payment_due_days: { type: integer, example: 14 }
        payment_due_unit: { type: string, enum: [days, month], nullable: true, description: "Jednotka splatnosti (NULL = dny)" }
        project_number: { type: string, nullable: true }
        contract_number: { type: string, nullable: true }
        budget_total:   { type: number, nullable: true }
        budget_yearly:  { type: number, nullable: true }
        budget_monthly: { type: number, nullable: true }
        default_revenue_category_id: { type: integer, nullable: true, description: "Default kategorie tržby pro faktury zakázky" }
        archived_at: { type: string, format: date-time, nullable: true }

    Project:
      allOf:
        - $ref: '#/components/schemas/ProjectBrief'
        - type: object
          properties:
            client_id: { type: integer, format: int64 }
            client_company_name: { type: string }
            client_main_email: { type: string, format: email, nullable: true }
            supplier_id: { type: integer }
            note: { type: string, nullable: true }
            requires_work_report_approval: { type: boolean }
            billing_emails:
              type: array
              maxItems: 3
              items: { $ref: '#/components/schemas/ProjectBilingEmail' }
            billing_emails_mode:
              type: string
              enum: [auto, append, replace]
              default: auto
              description: "Jak kombinovat e-maily zakázky s kontakty klienta (#86): auto = dosavadní per-typ chování (doklady/upomínky přidat, schvalování nahradit), append = vždy přidat, replace = vždy nahradit."
            created_at: { type: string, format: date-time }
            updated_at: { type: string, format: date-time }

    ProjectDetail:
      allOf:
        - $ref: '#/components/schemas/Project'
        - type: object
          properties:
            invoices_count:
              type: integer
              description: Počet faktur navázaných na zakázku.
            revenue_by_month:
              type: array
              items:
                type: object
                properties:
                  month: { type: string, example: "2026-04" }
                  currency: { type: string, example: CZK }
                  total: { type: number, format: float }
            revenue_by_year:
              type: array
              items:
                type: object
                properties:
                  year: { type: integer, example: 2026 }
                  currency: { type: string, example: CZK }
                  total: { type: number, format: float }
                  count: { type: integer }
            unpaid_summary:
              type: array
              items:
                type: object
                properties:
                  currency: { type: string, example: CZK }
                  unpaid_total:  { type: number, format: float }
                  unpaid_count:  { type: integer }
                  overdue_total: { type: number, format: float }
                  overdue_count: { type: integer }

    ProjectInput:
      type: object
      required: [client_id, name]
      properties:
        client_id:
          type: integer
          format: int64
          description: Musí patřit aktuálnímu supplier-ovi.
        name:
          type: string
          maxLength: 190
          example: "Webové stránky 2026"
        status:
          type: string
          enum: [active, paused, closed]
          default: active
        currency_id:
          type: integer
          description: Default = výchozí měna supplier-a.
        currency:
          type: string
          deprecated: true
          description: "Legacy — kód měny (CZK…). Resolvuje se na currency_id. Preferuj currency_id."
        hourly_rate:
          type: number
          format: float
          example: 1500.00
        payment_due_days:
          type: integer
          minimum: 1
          maximum: 365
          example: 14
        payment_due_unit:
          type: string
          enum: [days, month]
          nullable: true
        default_revenue_category_id:
          type: integer
          nullable: true
          description: "Default kategorie tržby (musí patřit tenantovi); zpětně doplní existující faktury zakázky."
        project_number:
          type: string
          maxLength: 50
          nullable: true
        contract_number:
          type: string
          maxLength: 50
          nullable: true
        budget_total:
          type: number
          format: float
          nullable: true
        budget_yearly:
          type: number
          format: float
          nullable: true
        budget_monthly:
          type: number
          format: float
          nullable: true
        requires_work_report_approval:
          type: boolean
          default: false
          description: |
            Pokud `true`, faktury z této zakázky vyžadují schválení výkazu
            víceprací zákazníkem před vystavením.
        note:
          type: string
          nullable: true
        billing_emails:
          type: array
          maxItems: 3
          items: { $ref: '#/components/schemas/ProjectBilingEmail' }
          description: |
            Až 3 fakturační emaily (slot 1/2/3). Kombinace s kontakty/hlavním
            e-mailem klienta se řídí přes `billing_emails_mode` (default `auto`
            = dosavadní chování: u dokladů a upomínek se přidávají, u schvalování
            výkazů nahrazují).
        billing_emails_mode:
          type: string
          enum: [auto, append, replace]
          default: auto
          description: "Jak kombinovat e-maily zakázky s příjemci dle kontaktů klienta (#86)."

    WorkReportItem:
      type: object
      properties:
        id: { type: integer }
        description: { type: string, example: "Analýza požadavků a tech-spec" }
        hours: { type: number, format: float, example: 8.5 }
        rate:  { type: number, format: float, example: 1500 }
        total_amount: { type: number, format: float, example: 12750, description: hours × rate }
        order_index: { type: integer, example: 1 }
        work_date: { type: string, format: date, nullable: true, description: "Datum provedení úkonu (volitelné)" }

    WorkReportItemInput:
      type: object
      required: [description, hours, rate]
      properties:
        description: { type: string }
        hours: { type: number, format: float, minimum: 0 }
        rate:  { type: number, format: float, minimum: 0 }
        order_index: { type: integer }
        work_date: { type: string, format: date, description: "Datum provedení úkonu (volitelné, YYYY-MM-DD)" }

    WorkReport:
      type: object
      properties:
        id: { type: integer, format: int64 }
        invoice_id: { type: integer, format: int64 }
        project_id: { type: integer, format: int64, nullable: true }
        title: { type: string, example: "Květen 2026 — vývoj backendu" }
        total_hours: { type: number, format: float }
        total_amount: { type: number, format: float }
        items:
          type: array
          items: { $ref: '#/components/schemas/WorkReportItem' }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    WorkReportInput:
      type: object
      required: [title, items]
      properties:
        project_id:
          type: integer
          nullable: true
          description: |
            Volitelné — pokud faktura má `project_id`, výkaz se na něj naváže
            automaticky. Pro faktury bez zakázky nechej `null`.
        title:
          type: string
          maxLength: 190
        items:
          type: array
          minItems: 1
          items: { $ref: '#/components/schemas/WorkReportItemInput' }

    PdfArchiveEntry:
      type: object
      description: |
        Jeden snapshot vystaveného PDF. Soubor se ukládá s timestampem a typem
        akce (issue / send / reissue) v `storage/invoices-pdf/`. Pro stažení
        použij `GET /api/v1/invoices/{id}/pdfs/{archiveId}`.
      properties:
        id: { type: integer, format: int64 }
        invoice_id: { type: integer, format: int64 }
        kind:
          type: string
          enum: [issue, send, reissue, manual]
          description: Co snapshot iniciovalo
        filename: { type: string, example: "F2026-001.pdf" }
        size_bytes: { type: integer }
        sha256: { type: string, example: "9f86d081…" }
        created_at: { type: string, format: date-time }
        created_by_user_id: { type: integer, nullable: true }

    InvoiceAttachment:
      type: object
      properties:
        id: { type: integer, format: int64 }
        invoice_id: { type: integer, format: int64 }
        filename: { type: string, example: "podklad-vykaz.xlsx" }
        mime_type: { type: string, example: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" }
        size_bytes: { type: integer, example: 18452 }
        created_at: { type: string, format: date-time }

    AresLookupResult:
      type: object
      description: |
        Odpověď ARES lookup-u. Pokud IČO existuje, `found = true` (implicitní) a
        ostatní pole jsou vyplněná. Pro neexistující IČO vrátíme `{ found: false }`.
      properties:
        found: { type: boolean, example: true }
        source:
          type: string
          enum: [fresh, cache]
          description: Zda data přišla z ARES API nebo z 24h cache
        company_name: { type: string, example: "MyWebdesign.cz s.r.o." }
        ic:  { type: string, example: "27082440" }
        dic: { type: string, example: "CZ27082440" }
        street: { type: string }
        city: { type: string }
        zip: { type: string, example: "301 00" }
        country_iso2: { type: string, example: "CZ" }
        is_vat_payer: { type: boolean }
        date_active: { type: string, format: date, description: "Datum vzniku firmy" }
        legal_form: { type: string, example: "112" }

    ViesLookupResult:
      type: object
      properties:
        valid: { type: boolean }
        vat_id: { type: string, example: "DE123456789" }
        country_code: { type: string, example: "DE" }
        name: { type: string, nullable: true }
        address: { type: string, nullable: true }
        consultation_number:
          type: string
          nullable: true
          description: |
            VIES consultation number — důkazní záznam pro daňovou kontrolu, že DIČ
            byl k danému datu platný.
        request_date: { type: string, format: date-time }
        source:
          type: string
          enum: [fresh, cache]

    ActivityLogEntry:
      type: object
      properties:
        id: { type: integer, format: int64 }
        action:
          type: string
          example: "invoice.issued"
          description: "Tečka-separátor: entity.verb (např. invoice.paid, project.archived, auth.login)"
        entity_type: { type: string, nullable: true, example: "invoice" }
        entity_id:   { type: integer, format: int64, nullable: true }
        user_id:     { type: integer, format: int64, nullable: true, description: "Null = systémová akce (cron)" }
        user_email:  { type: string, format: email, nullable: true }
        payload:
          type: object
          additionalProperties: true
          nullable: true
          description: Akce-specifické metadata (citlivá pole se redaktují)
        ip:         { type: string, nullable: true, example: "203.0.113.42" }
        user_agent: { type: string, nullable: true }
        created_at: { type: string, format: date-time }

    DashboardSummary:
      type: object
      description: |
        Souhrnná data pro Dashboard (homepage) i stránku Grafy (`/stats`).

        **VAT-aware obrat:** Pro plátce DPH (`supplier.is_vat_payer = 1`) se obrat
        ve všech KPI a grafech počítá z `total_without_vat`; pro neplátce z
        `total_with_vat`. Flag `is_vat_payer` v response signalizuje použitou
        variantu.
      properties:
        today: { type: string, format: date }
        year: { type: integer, example: 2026 }
        prev_year: { type: integer, example: 2025 }
        is_vat_payer:
          type: boolean
          description: "Plátce DPH? Určuje použitý sloupec obratu (total_without_vat vs total_with_vat)."
        kpi:
          type: object
          properties:
            per_currency:
              type: array
              description: "Per měna: obrat YTD + loni + YTD loni (do stejného kalendářního dne) + počty faktur/klientů/zakázek pro každý rok."
              items:
                type: object
                properties:
                  currency: { type: string }
                  this_year: { type: number }
                  prev_year: { type: number }
                  prev_year_ytd: { type: number, description: "Obrat loni do stejného dne v roce (pro fair YoY)" }
                  change_pct: { type: number, nullable: true, description: "Letošek vs prev_year_ytd v %" }
                  this_year_invoice_count: { type: integer }
                  prev_year_invoice_count: { type: integer }
                  this_year_client_count: { type: integer, description: "DISTINCT klienti v této měně (letos)" }
                  prev_year_client_count: { type: integer }
                  this_year_project_count: { type: integer, description: "DISTINCT zakázky v této měně (letos)" }
                  prev_year_project_count: { type: integer }
            issued_count_ytd: { type: integer }
            overdue_count: { type: integer }
            overdue_per_currency:
              type: array
              items:
                type: object
                properties:
                  currency: { type: string }
                  count: { type: integer }
                  total: { type: number }
            avg_payment_days: { type: number, nullable: true }
            status_counts_ytd:
              type: object
              additionalProperties: { type: integer }
        overdue:
          type: array
          items: { $ref: '#/components/schemas/Invoice' }
        unpaid_upcoming:
          type: array
          items: { $ref: '#/components/schemas/Invoice' }
        draft_invoices:
          type: array
          description: "Rozpracované (status=draft) vystavené faktury pro widget Výkazy práce na Přehledu — firma + zakázka + odkaz na editaci. Max 24, řazeno dle updated_at DESC."
          items:
            type: object
            properties:
              id: { type: integer }
              varsymbol: { type: string, nullable: true }
              invoice_type: { type: string }
              client_id: { type: integer }
              client_company_name: { type: string }
              project_id: { type: integer, nullable: true }
              project_name: { type: string, nullable: true, description: "Název zakázky (NULL = bez zakázky)." }
              currency: { type: string }
              issue_date: { type: string, format: date }
              total_with_vat: { type: number }
        top_clients_ytd:
          type: array
          description: "Top 12 klientů YTD per měna."
          items: { $ref: '#/components/schemas/TopClient' }
        top_clients_prev_year:
          type: array
          description: "Top 12 klientů za minulý rok per měna."
          items: { $ref: '#/components/schemas/TopClient' }
        top_clients_12m:
          type: array
          description: "Top 12 klientů za rolling posledních 12 měsíců. Robustní vůči začátku roku (kdy YTD je téměř prázdné)."
          items: { $ref: '#/components/schemas/TopClient' }
        revenue_by_month:
          type: array
          description: "Per měna: rolling 12 měsíců (`months`) + odpovídajících 12 měsíců o rok dříve (`prev_year`)."
          items:
            type: object
            properties:
              currency: { type: string }
              months:
                type: array
                items: { $ref: '#/components/schemas/MonthBucket' }
              prev_year:
                type: array
                items: { $ref: '#/components/schemas/MonthBucket' }
        revenue_by_year:
          type: array
          description: "Obrat po rocích — všechny roky s aktivitou, VAT-aware."
          items:
            type: object
            properties:
              year: { type: integer }
              currency: { type: string }
              total: { type: number }
              invoice_count: { type: integer }
        rolling_12m:
          type: array
          description: "Plovoucí 12měsíční obrat (rolling) per měna + 12 měsíců předtím (pro rolling YoY srovnání). Relevantní pro DPH limit 2 000 000 Kč."
          items:
            type: object
            properties:
              currency: { type: string }
              total: { type: number, description: "Obrat za posledních 12 měsíců (rolling)" }
              prev_period_total: { type: number, description: "Obrat za 12 měsíců předtím" }
        revenue_last_30d:
          type: array
          description: "Live indikátor obratu za posledních 30 dní per měna."
          items:
            type: object
            properties:
              currency: { type: string }
              total: { type: number }
              invoice_count: { type: integer }
        revenue_forecast:
          type: array
          description: |
            Forecast ročního obratu — medián tří nezávislých projekcí (odolný vůči odlehlému signálu).

            **Postup:** medián ze tří signálů —
            (1) run-rate `ytd / uplynulé_dny × dní_v_roce`,
            (2) sezonalita × krátkodobý růst `ytd + prev_year_remainder × growth_short`,
            (3) sezonalita × dlouhodobý trend `ytd + prev_year_remainder × growth_trend`.
            `forecast` je medián, `forecast_low`/`forecast_high` rozpětí (min/max projekcí).
            Bez loňských dat (`prev_year_full = 0`) se použije pouze run-rate.
          items:
            type: object
            properties:
              currency: { type: string }
              ytd: { type: number, description: "Obrat letos Jan 1 – dnes" }
              prev_year_remainder: { type: number, description: "Obrat loni od dneška do konce roku (sezonalita)" }
              prev_year_full: { type: number, description: "Celý loňský rok pro srovnání" }
              growth_short: { type: number, description: "Krátkodobý růst: rolling 12m / předch. 12m, cap [0.5, 2.5]" }
              growth_trend: { type: number, description: "Dlouhodobý trend: CAGR z posledních ≤5 let, cap [0.8, 1.8]" }
              forecast: { type: number, description: "Medián tří projekcí — predikovaný celoroční obrat" }
              forecast_low: { type: number, description: "Nejnižší ze tří projekcí (spodní hranice rozpětí)" }
              forecast_high: { type: number, description: "Nejvyšší ze tří projekcí (horní hranice rozpětí)" }
        cashflow_ytd:
          type: array
          description: "Měsíční cash-flow (skutečné platby `paid_at`) letošek + minulý rok per měna. Doplňuje obrat o reálné inkaso."
          items:
            type: object
            properties:
              currency: { type: string }
              months:
                type: array
                items: { $ref: '#/components/schemas/MonthBucket' }
              prev_year:
                type: array
                items: { $ref: '#/components/schemas/MonthBucket' }
        cashflow_forecast:
          type: array
          description: "Kumulativní cash-flow forecast — co přiteče z neuhrazených faktur v příštích 30/60/90 dnech."
          items:
            type: object
            properties:
              currency: { type: string }
              in_30: { type: number }
              in_60: { type: number }
              in_90: { type: number }
              count_30: { type: integer }
              count_60: { type: integer }
              count_90: { type: integer }
        due_buckets:
          type: array
          description: "Splatnost neuhrazených faktur — kumulativně dnes / tento týden (Po-Ne) / tento měsíc."
          items:
            type: object
            properties:
              currency: { type: string }
              today_count: { type: integer }
              today_total: { type: number }
              week_count: { type: integer }
              week_total: { type: number }
              month_count: { type: integer }
              month_total: { type: number }
        aging_report:
          type: array
          description: "Aging report — stáří pohledávek po splatnosti v bucketech per měna."
          items:
            type: object
            properties:
              currency: { type: string }
              current: { type: number, description: "Ještě před splatností" }
              b1_30: { type: number, description: "1–30 dní po splatnosti" }
              b31_60: { type: number }
              b61_90: { type: number }
              b90_plus: { type: number, description: "Více než 90 dní po splatnosti" }
              current_n: { type: integer }
              b1_30_n: { type: integer }
              b31_60_n: { type: integer }
              b61_90_n: { type: integer }
              b90_plus_n: { type: integer }
        payment_days_histogram:
          type: object
          description: "Distribuce doby úhrady (`paid_at - issue_date`) za posledních 12 měsíců."
          properties:
            buckets:
              type: array
              items:
                type: object
                properties:
                  key: { type: string, enum: ["0-7", "8-14", "15-30", "30+"] }
                  label: { type: string }
                  count: { type: integer }
            total: { type: integer }
            avg_days: { type: number, nullable: true }
        vat_breakdown_12m:
          type: array
          description: |
            Rozpad obratu (base bez DPH) podle sazby DPH — **pouze pro plátce DPH**.
            Pro neplátce vrací prázdné pole. Per měna a `vat_rate_snapshot`.
            Reverse-charge řádky jsou vyznačeny labelem „RC (reverse charge)".
          items:
            type: object
            properties:
              label: { type: string, example: "21 %" }
              base: { type: number, description: "Suma total_without_vat" }
              currency: { type: string }
        invoice_size_histogram:
          type: object
          description: "Histogram velikosti faktur za posledních 12 měsíců. Buckety v CZK ekvivalentu (non-CZK přepočtené přes `exchange_rate`)."
          properties:
            buckets:
              type: array
              items:
                type: object
                properties:
                  key: { type: string, enum: ["0-5", "5-25", "25-100", "100+"] }
                  label: { type: string }
                  count: { type: integer }
                  total_czk: { type: number }
            total: { type: integer }
        active_clients_count:
          type: integer
          description: "Počet nearchivovaných klientů aktuálního dodavatele."
        active_recurring_count:
          type: integer
          description: "Počet aktivních šablon pravidelné fakturace."
        pending_approvals:
          type: object
          properties:
            requested: { type: integer }
            overdue: { type: integer, description: "Bez reakce > 5 dní" }

    TopClient:
      type: object
      properties:
        client_id: { type: integer }
        company_name: { type: string }
        currency: { type: string }
        total: { type: number }
        invoice_count: { type: integer }

    MonthBucket:
      type: object
      properties:
        ym: { type: string, example: "2026-04", description: "Měsíc ve formátu YYYY-MM" }
        total: { type: number }

    ProjectStats:
      type: object
      description: "Statistiky zakázek pro stránku Grafy. VAT-aware (viz `is_vat_payer`)."
      properties:
        this_year: { type: integer, example: 2026 }
        prev_year: { type: integer, example: 2025 }
        primary_currency:
          type: string
          description: "Nejvyužívanější měna ve fakturách — používá se k filtrování per-currency agregací."
        is_vat_payer: { type: boolean }
        top_this_year: { $ref: '#/components/schemas/ProjectStatsBlock' }
        top_prev_year: { $ref: '#/components/schemas/ProjectStatsBlock' }
        top_12m:
          allOf:
            - $ref: '#/components/schemas/ProjectStatsBlock'
            - description: "Top 12 zakázek za rolling posledních 12 měsíců. Robustní vůči začátku roku."
        totals_per_year:
          type: array
          items:
            type: object
            properties:
              year: { type: integer }
              currency: { type: string }
              total: { type: number }
              invoice_count: { type: integer }
        status_breakdown:
          type: array
          items:
            type: object
            properties:
              status: { type: string, enum: [active, paused, closed] }
              count: { type: integer }

    ProjectStatsBlock:
      type: object
      properties:
        top:
          type: array
          items:
            type: object
            properties:
              id: { type: integer }
              name: { type: string }
              client_company_name: { type: string }
              revenue: { type: number }
              invoice_count: { type: integer }
        others:
          type: object
          description: 'Agregovaná položka "Ostatní" (mimo top 12).'
          properties:
            revenue: { type: number }
            count: { type: integer }

    BankStatement:
      type: object
      description: "Položka seznamu výpisů (GET /bank-statements). Bankovní spojení dodavatele je per-měna na /settings/currencies, ne tady."
      properties:
        id: { type: integer, format: int64 }
        file_name: { type: string, example: "vypis_2026_05.gpc" }
        account_number: { type: string, example: "1234567890/0100" }
        currency: { type: string, nullable: true, example: EUR, description: "ISO 4217. Odvozeno z transakcí výpisu nebo z currencies.account_number. NULL u výpisů importovaných před v4.1.0." }
        statement_date: { type: string, format: date }
        statement_number: { type: string, nullable: true }
        prev_balance: { type: number, format: float }
        curr_balance: { type: number, format: float }
        transaction_count: { type: integer }
        matched_count: { type: integer }
        imported_at: { type: string, format: date-time }
        has_file: { type: boolean, description: "True pokud server má uložený originál výpisu — ke stažení přes /bank-statements/{id}/download." }
        has_pdf: { type: boolean, description: "True pokud je nahrán doprovodný PDF výpis." }
        pdf_name: { type: string, nullable: true }
        account_label: { type: string, nullable: true, description: "Odvozeno z currencies.label" }

    BankStatementDetail:
      allOf:
        - $ref: '#/components/schemas/BankStatement'
        - type: object
          properties:
            file_hash: { type: string, description: "SHA-256 originálního souboru" }
            bank_code: { type: string, nullable: true }
            credit_total: { type: number, format: float, nullable: true }
            debit_total: { type: number, format: float, nullable: true }
            imported_by: { type: integer, nullable: true, description: "User ID importujícího" }
            transactions:
              type: array
              items: { $ref: '#/components/schemas/BankTransaction' }

    BankTransaction:
      type: object
      properties:
        id: { type: integer, format: int64 }
        statement_id: { type: integer, format: int64 }
        posted_at: { type: string, format: date }
        amount: { type: number, format: float, example: 12500.00 }
        currency: { type: string, nullable: true, example: CZK }
        variable_symbol: { type: string, nullable: true }
        constant_symbol: { type: string, nullable: true }
        specific_symbol: { type: string, nullable: true }
        counterparty_account: { type: string, nullable: true }
        counterparty_bank: { type: string, nullable: true }
        counterparty_name: { type: string, nullable: true }
        description: { type: string, nullable: true }
        bank_ref: { type: string, nullable: true, description: "Bankovní referenční ID transakce" }
        match_status:
          type: string
          enum: [unmatched, auto_exact, auto_partial, manual, ignored]
          description: "Stav párování. auto_* = automatika, manual = ruční přiřazení."
        matched_invoice_id: { type: integer, format: int64, nullable: true, description: "Spárovaná VYDANÁ faktura" }
        matched_purchase_invoice_id: { type: integer, format: int64, nullable: true, description: "Spárovaná PŘIJATÁ faktura (z payment_matches; jen v detailu)" }
        matched_varsymbol: { type: string, nullable: true }
        matched_invoice_amount: { type: number, format: float, nullable: true }
        client_id: { type: integer, nullable: true }
        matched_client_name: { type: string, nullable: true }
        matched_at: { type: string, format: date-time, nullable: true }
        matched_by: { type: integer, nullable: true, description: "User ID (u ručního párování)" }

    SupplierFull:
      type: object
      description: |
        Kompletní data dodavatele (per-supplier konfigurace). Vrací `SELECT s.*`
        + odvozená pole. Bankovní účty NEJSOU součástí — jsou per-měna
        na `GET /api/settings/currencies`. (Pozn.: response obsahuje i další
        sloupce mimo tento výčet — branding, integrace, payment-thanks ap.)
      properties:
        id: { type: integer }
        company_name: { type: string }
        display_name: { type: string, nullable: true }
        street: { type: string }
        city: { type: string }
        zip: { type: string }
        country_id: { type: integer }
        country_iso: { type: string, description: "Odvozený ISO2 kód země" }
        ic: { type: string, nullable: true }
        dic: { type: string, nullable: true }
        is_vat_payer: { type: boolean }
        is_identified: { type: boolean, description: "Identifikovaná osoba (§ 6g–6l ZDPH, issue #94) — neplátce v tuzemsku s přeshraničními povinnostmi (RC faktury do EU, SHV, samovyměření bez odpočtu). Nelze kombinovat s is_vat_payer=true." }
        commercial_register: { type: string, nullable: true, description: "Spisová značka u rejstříkového soudu (doplňuje ARES enricher)" }
        email: { type: string, format: email, nullable: true }
        phone: { type: string, nullable: true }
        web: { type: string, nullable: true }
        tagline: { type: string, nullable: true }
        default_currency_id: { type: integer }
        default_currency: { type: string, description: "Odvozený kód výchozí měny" }
        default_payment_due_days: { type: integer }
        default_payment_due_unit: { type: string, enum: [days, month] }
        default_hourly_rate: { type: number, format: float }
        default_prices_include_vat: { type: boolean, description: "Default pro nové doklady (migrace 0083)" }
        invoice_number_format:
          type: string
          nullable: true
          example: "F{YYYY}-{####}"
          description: "{YYYY}, {MM}, {####} placeholders. Per-supplier."
        proforma_number_format: { type: string, nullable: true }
        credit_note_number_format: { type: string, nullable: true }
        taxpayer_type: { type: string, enum: [fo, po], nullable: true, description: "Fyzická / právnická osoba (EPO výkazy)" }
        vat_period: { type: string, enum: [monthly, quarterly], nullable: true }
        financial_office_code: { type: string, nullable: true, description: "Kód FÚ pro EPO (= CRPDPH cisloFu)" }
        flat_tax_band: { type: string, enum: [none, band1, band2, band3], description: "Pásmo paušální daně" }
        email_branding_enabled: { type: boolean }
        email_accent_color: { type: string, example: "#3B2D83" }
        has_email_logo: { type: boolean }
        auto_send_reminders: { type: boolean }
        reminder_days_after_due: { type: integer, minimum: 1, maximum: 365, default: 3, description: "Per-dodavatel práh: po kolika dnech po splatnosti poslat první automatickou upomínku (migrace 0089). CLI --days ho přebije." }
        self_copy:
          type: object
          nullable: true
          description: |
            Kopie odchozích e-mailů na e-mail dodavatele (migrace 0102).
            Klíče = typy zpráv, hodnoty off|cc|bcc. Chybějící klíč nebo null
            = fallback na globální cfg flagy (cc_supplier_on_*). Klíč
            `approvals` platí pro žádost o schválení i schvalovací upomínku.
          properties:
            documents: { type: string, enum: ["off", cc, bcc] }
            reminders: { type: string, enum: ["off", cc, bcc] }
            approvals: { type: string, enum: ["off", cc, bcc] }
          example: { documents: cc, approvals: bcc }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    ProjectsPagination:
      type: object
      properties:
        total:    { type: integer, example: 423 }
        page:     { type: integer, example: 1 }
        per_page: { type: integer, example: 50 }
        pages:    { type: integer, example: 9 }

    InvoicePayment:
      type: object
      description: "Evidovaná platba faktury / zálohové faktury (#89). N:1 — jedna faktura může mít víc plateb (splátky, více převodů, avíza)."
      properties:
        id: { type: integer, format: int64 }
        invoice_id: { type: integer, format: int64 }
        paid_on: { type: string, format: date }
        amount: { type: number, format: float, description: "Částka v měně faktury (> 0)" }
        currency: { type: string, example: CZK, description: "Denormalizovaný kód měny faktury v okamžiku platby" }
        variable_symbol: { type: string, nullable: true }
        bank_reference: { type: string, nullable: true }
        note: { type: string, nullable: true }
        source:
          type: string
          enum: [manual, mark_paid, bank, legacy]
          description: "manual = ruční záznam, mark_paid = zkratka „označit zaplaceno“, bank = bankovní párování (GPC i e-mailové avízo), legacy = backfill z dob před evidencí plateb"
        bank_transaction_id: { type: integer, format: int64, nullable: true, description: "Vazba na bankovní transakci (taková platba se maže přes unmatch ve výpisu)" }
        bank_statement_id: { type: integer, format: int64, nullable: true }
        tax_document_invoice_id: { type: integer, format: int64, nullable: true, description: "Daňový doklad k přijaté platbě vystavený k této platbě (jen u záloh)" }
        tax_document_varsymbol: { type: string, nullable: true }
        tax_document_status: { type: string, nullable: true, enum: [draft, issued, sent, reminded, paid, cancelled] }
        created_by: { type: integer, nullable: true }
        created_at: { type: string, format: date-time }

    InvoiceItem:
      type: object
      properties:
        id: { type: integer }
        invoice_id: { type: integer }
        description: { type: string }
        quantity: { type: number, format: float, description: "Může být záporné pro slevový řádek. Nesmí být 0." }
        unit: { type: string, example: ks }
        unit_price_without_vat: { type: number, format: float, description: "Jednotková cena bez DPH. Může být záporná pro slevový řádek." }
        vat_rate_id: { type: integer, description: "FK do /codebooks/vat-rates (kontextově k supplierovi)" }
        vat_rate_snapshot:
          type: number
          format: float
          example: 21
          description: "Snapshot procentní hodnoty DPH v okamžiku přidání položky (drží se i po případné změně sazby v číselníku)"
        vat_code: { type: string, nullable: true, example: "STD", description: "Kód sazby ze snapshot-u (STD / RED1 / RED2 / ZERO / FREE)" }
        vat_label_cs: { type: string, nullable: true, description: "Lokalizovaný název sazby (CS)" }
        vat_label_en: { type: string, nullable: true, description: "Lokalizovaný název sazby (EN)" }
        vat_classification_code: { type: string, nullable: true, example: "3", description: "Kód klasifikace plnění pro DPH přiznání / Kontrolní hlášení (default podle sazby + reverse charge + země klienta). U neplátce DPH bez významu." }
        total_without_vat: { type: number, format: float }
        total_vat: { type: number, format: float }
        total_with_vat: { type: number, format: float }
        item_kind:
          type: string
          enum: [standard, discount]
          default: standard
          description: "`discount` = systémově vygenerovaný záporný řádek „Sleva X %\" z `discount_percent` faktury (po sazbách DPH). Needituj ho ručně."
        order_index: { type: integer }
        linked_work_report_id: { type: integer, nullable: true }

    InvoiceItemInput:
      type: object
      required: [description, quantity, unit_price_without_vat, vat_rate_id]
      properties:
        description: { type: string }
        quantity: { type: number, format: float, description: "Nesmí být 0. Může být záporné pro slevový řádek, ale ne současně se zápornou cenou." }
        unit: { type: string, default: "ks" }
        unit_price_without_vat: { type: number, format: float, description: "Jednotková cena BEZ DPH. Může být záporná pro slevový řádek, ale ne současně se záporným množstvím." }
        vat_rate_id: { type: integer, description: "FK do /codebooks/vat-rates (kontextově k supplierovi)" }
        order_index: { type: integer, description: "Pořadí v rámci faktury. Default = pozice v poli." }
        vat_classification_code: { type: string, nullable: true, description: "Volitelný kód klasifikace plnění (DPH přiznání / KH). Když nevyplněno, server dopočítá default podle sazby + reverse charge + země klienta." }

    RecurringTemplate:
      type: object
      description: "Šablona pravidelné fakturace. Cron `bin/cron-generate-recurring-invoices.php` generuje faktury dle periodicity."
      properties:
        id: { type: integer }
        supplier_id: { type: integer }
        client_id: { type: integer }
        client_company_name: { type: string }
        project_id: { type: integer, nullable: true }
        project_name: { type: string, nullable: true }
        name: { type: string, description: "Display name pro admin (např. \"Hosting Acme s.r.o.\")" }
        frequency:
          type: string
          enum: [monthly, quarterly, semi_annually, annually]
        day_of_month:
          type: integer
          nullable: true
          description: "1–28; pokud null a end_of_month=false, odvodí se z anchor_date"
        end_of_month:
          type: boolean
          description: "true = poslední den měsíce (28/29/30/31 dynamicky); přebíjí day_of_month"
        anchor_date: { type: string, format: date }
        end_date:    { type: string, format: date, nullable: true }
        next_run_date: { type: string, format: date }
        last_run_date: { type: string, format: date, nullable: true }
        invoice_type:
          type: string
          enum: [invoice, proforma]
        currency_id: { type: integer }
        currency: { type: string, example: CZK }
        language: { type: string, enum: [cs, en] }
        payment_method:
          type: string
          enum: [bank_transfer, card, cash, other]
        reverse_charge: { type: boolean }
        payment_due_days: { type: integer }
        discount_percent: { type: number, format: float, description: "Procentuální sleva (0–100) zděděná na každou vygenerovanou fakturu." }
        revenue_category_id:
          type: integer
          nullable: true
          description: "Pevná kategorie tržby pro generované faktury (#119) — přebíjí fallback výchozí kategorie zakázky > zákazníka. NULL = fallback. Na fakturu se ukládá jako snapshot při generování."
        revenue_category_label: { type: string, nullable: true, description: "Label kategorie (jen detail šablony)." }
        revenue_category_code: { type: string, nullable: true, description: "Kód kategorie (jen detail šablony)." }
        draft_open_mode:
          type: string
          enum: [at_issue, period_start]
          description: "at_issue = koncept vznikne až v den vystavení; period_start = koncept se otevře na začátku období (průběžný výkaz víceprací) a vystaví se na konci."
        reminder_days_before: { type: integer, description: "0–14; připomenutí konceptu N dní před vystavením (0 = vypnuto; jen pro period_start)." }
        note_above_items: { type: string, nullable: true }
        note_below_items: { type: string, nullable: true }
        increment_month_in_descriptions:
          type: boolean
          description: "Posun M/YYYY v popiscích položek (např. „Hosting 3/2026\" → „Hosting 4/2026\"). Pro frequency != monthly se posouvá o odpovídající počet měsíců."
        auto_issue:
          type: boolean
          description: "true = cron rovnou vystaví (varsymbol + snapshot, status=issued); false = nechá draft"
        auto_send_email:
          type: boolean
          description: "true = po vystavení rovnou odešle klientovi; vyžaduje auto_issue=true"
        status:
          type: string
          enum: [active, paused, expired]
        invoices_generated_count: { type: integer }
        last_error: { type: string, nullable: true, description: "Poslední chyba generování (cron) — UI ji ukáže jako banner u šablony; vyčistí se po úspěšném ručním běhu." }
        last_error_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string }
        updated_at: { type: string }
        items:
          type: array
          items:
            type: object
            properties:
              id: { type: integer }
              description: { type: string }
              quantity: { type: number, format: float, description: "Může být záporné pro slevový řádek. Nesmí být 0." }
              unit: { type: string }
              unit_price_without_vat: { type: number, format: float, description: "Může být záporná pro slevový řádek." }
              vat_rate_id: { type: integer }
              vat_rate_percent: { type: number, format: float }
              order_index: { type: integer }

    RecurringTemplateInput:
      type: object
      required: [client_id, name, frequency, anchor_date, currency_id, items]
      properties:
        client_id: { type: integer }
        project_id: { type: integer, nullable: true }
        name: { type: string, maxLength: 200 }
        frequency:
          type: string
          enum: [monthly, quarterly, semi_annually, annually]
        day_of_month:
          type: integer
          minimum: 1
          maximum: 28
          nullable: true
        end_of_month: { type: boolean, default: false }
        anchor_date: { type: string, format: date }
        end_date:    { type: string, format: date, nullable: true }
        invoice_type:
          type: string
          enum: [invoice, proforma]
          default: invoice
        currency_id: { type: integer }
        language: { type: string, enum: [cs, en], default: cs }
        payment_method:
          type: string
          enum: [bank_transfer, card, cash, other]
          default: bank_transfer
        reverse_charge: { type: boolean, default: false }
        payment_due_days: { type: integer, default: 14 }
        discount_percent:
          type: number
          format: float
          minimum: 0
          maximum: 100
          default: 0
          description: "Procentuální sleva z celé faktury (0–100); zdědí ji každá vygenerovaná faktura (materializuje se jako záporná položka „Sleva X %\")."
        revenue_category_id:
          type: integer
          nullable: true
          description: "Pevná kategorie tržby pro generované faktury (#119). Musí existovat a patřit dodavateli šablony. NULL/vynecháno = fallback výchozí kategorie zakázky > zákazníka při generování."
        draft_open_mode:
          type: string
          enum: [at_issue, period_start]
          default: at_issue
          description: "period_start (koncept na začátku období → vystavení na konci) vyžaduje auto_issue=true."
        tax_date_mode:
          type: string
          enum: [same_as_issue, previous_month_last_day]
          default: same_as_issue
          description: "DUZP vygenerované faktury: stejné jako datum vystavení, nebo poslední den předchozího měsíce."
        reminder_days_before:
          type: integer
          minimum: 0
          maximum: 14
          default: 0
          description: "Připomenutí konceptu N dní před vystavením (jen draft_open_mode=period_start; 0 = vypnuto)."
        note_above_items: { type: string, nullable: true }
        note_below_items: { type: string, nullable: true }
        increment_month_in_descriptions: { type: boolean, default: true }
        auto_issue: { type: boolean, default: true }
        auto_send_email: { type: boolean, default: true }
        items:
          type: array
          items:
            type: object
            required: [description, quantity, unit_price_without_vat, vat_rate_id]
            properties:
              description: { type: string }
              quantity: { type: number, format: float, description: "Nesmí být 0. Může být záporné pro slevový řádek, ale ne současně se zápornou cenou." }
              unit: { type: string, default: ks }
              unit_price_without_vat: { type: number, format: float, description: "Může být záporná pro slevový řádek, ale ne současně se záporným množstvím." }
              vat_rate_id: { type: integer }
              order_index: { type: integer }

    # ========================================================================
    # Purchase invoices (přijaté faktury) — fáze 1 integrace forku
    # ========================================================================
    PurchaseInvoiceListItem:
      type: object
      properties:
        id: { type: integer }
        supplier_id: { type: integer }
        vendor_id: { type: integer }
        varsymbol: { type: string, nullable: true, description: "Naše interní číslo PF-YYYYMM-NNNN" }
        vendor_invoice_number: { type: string }
        document_kind: { type: string, enum: [invoice, receipt, credit_note, advance] }
        issue_date: { type: string, format: date }
        tax_date: { type: string, format: date, nullable: true, description: "DUZP" }
        due_date: { type: string, format: date }
        received_at: { type: string, format: date }
        currency_id: { type: integer }
        currency: { type: string }
        total_without_vat: { type: number }
        total_vat: { type: number }
        total_with_vat: { type: number }
        advance_paid_amount: { type: number }
        amount_to_pay: { type: number, description: "Generated column = total_with_vat - advance_paid_amount" }
        exchange_rate: { type: number, nullable: true }
        exchange_rate_date: { type: string, format: date, nullable: true }
        status: { type: string, enum: [draft, received, booked, paid, cancelled] }
        booked_at: { type: string, format: date-time, nullable: true }
        paid_at: { type: string, format: date, nullable: true }
        cancelled_at: { type: string, format: date-time, nullable: true }
        vat_deduction: { type: string, enum: [full, none, proportional] }
        vat_deduction_percent: { type: number, format: float }
        tax_deductible: { type: boolean }
        extraction_warning: { type: string, nullable: true }
        currency_symbol: { type: string }
        vendor_company_name: { type: string }
        vendor_ic: { type: string, nullable: true }
        month_bucket: { type: string, example: "2026-05" }

    PurchaseInvoiceItem:
      type: object
      properties:
        id: { type: integer }
        purchase_invoice_id: { type: integer }
        description: { type: string }
        quantity: { type: number }
        unit: { type: string }
        unit_price_without_vat: { type: number }
        vat_rate_id: { type: integer }
        vat_rate_snapshot: { type: number }
        total_without_vat: { type: number }
        total_vat: { type: number }
        total_with_vat: { type: number }
        order_index: { type: integer }
        vat_code: { type: string, nullable: true, description: "Kód sazby ze snapshot-u (STD / RED1 / …)" }
        vat_label_cs: { type: string, nullable: true }
        vat_label_en: { type: string, nullable: true }
        vat_classification_code:
          type: string
          nullable: true
          description: "MFČR kód pro DPH/KH (připraveno pro fázi 6)"
        is_fixed_asset:
          type: boolean
          default: false
          description: |
            Řádek je za dlouhodobý majetek (§ 4 odst. 4 písm. c — vozidlo, stroj, ...).
            Per-řádek override umožňuje mixed doklady (pár položek majetek, zbytek běžné).
            Pokud je nastaveno na řádku, vyhrává nad `PurchaseInvoice.is_fixed_asset` (header).
            Hodnota se na DPHDP3 navíc uvede na ř. 47 (`nar_maj`).

    PurchaseInvoiceItemPayload:
      type: object
      required: [description, quantity, unit_price_without_vat, vat_rate_id]
      properties:
        description: { type: string }
        quantity: { type: number }
        unit: { type: string, default: ks }
        unit_price_without_vat: { type: number }
        vat_rate_id: { type: integer }
        order_index: { type: integer }
        vat_classification_code: { type: string, nullable: true }
        is_fixed_asset: { type: boolean, default: false }

    PurchaseInvoice:
      type: object
      properties:
        id: { type: integer }
        supplier_id: { type: integer }
        vendor_id: { type: integer }
        varsymbol: { type: string, nullable: true }
        vendor_invoice_number: { type: string }
        document_kind: { type: string, enum: [invoice, receipt, credit_note, advance] }
        advance_purchase_invoice_id: { type: integer, nullable: true, description: "FK na zálohovou fakturu, kterou tento doklad vyúčtovává (párování zálohy)" }
        advance_link_suggested_id: { type: integer, nullable: true, description: "AI/auto návrh na propojení zálohy (čeká na potvrzení)" }
        issue_date: { type: string, format: date }
        tax_date: { type: string, format: date, nullable: true }
        due_date: { type: string, format: date }
        received_at: { type: string, format: date }
        prices_include_vat: { type: boolean, description: "Ceny položek zadané včetně DPH (migrace 0083)" }
        expense_category_id: { type: integer, nullable: true, description: "FK do /expense-categories — kategorie nákladu pro CRM/dashboard" }
        expense_category_label: { type: string, nullable: true }
        expense_category_code: { type: string, nullable: true }
        currency_id: { type: integer }
        currency: { type: string }
        currency_symbol: { type: string }
        exchange_rate: { type: number, nullable: true }
        exchange_rate_date: { type: string, format: date, nullable: true }
        exchange_rate_source: { type: string, enum: [cnb, manual, idoklad, fakturoid] }
        reverse_charge: { type: boolean }
        vat_deduction:
          type: string
          enum: [full, none, proportional]
          default: full
          description: |
            Nárok na odpočet DPH. `none` = faktura NEvstupuje do Knihy DPH ani do
            DPHDP3 / KH (jen účetní náklad). `proportional` = krácený / poměrný odpočet
            (§75) — odpočet (základ i daň) se zkrátí o `vat_deduction_percent`.
        vat_deduction_percent:
          type: number
          format: float
          minimum: 0
          maximum: 100
          default: 100
          description: "Procento odpočtu při `vat_deduction='proportional'` (§75). Pro full/none se ignoruje (drží 100)."
        tax_deductible:
          type: boolean
          default: true
          description: "Daňová uznatelnost nákladu pro daň z příjmů (DPFO/DPPO); false = nezahrne se do nákladů. Nezávislé na DPH."
        is_fixed_asset:
          type: boolean
          default: false
          description: |
            Doklad je za dlouhodobý majetek (§ 4 odst. 4 písm. c) — vozidlo, stroj.
            Header flag pro celý doklad. Pro mixed doklady (část majetek, část běžné
            náklady) použij per-řádek `PurchaseInvoiceItem.is_fixed_asset`.

            Když je nastaveno a doklad jde do odpočtu na ř. 40-45 DPHDP3 (přímo
            nebo přes ř. 43 RC mirror), hodnota se navíc uvede na ř. 47 jako
            doplňující údaj (`nar_maj`).
        language: { type: string, enum: [cs, en] }
        note_above_items: { type: string, nullable: true }
        note_below_items: { type: string, nullable: true }
        vendor_snapshot:
          type: object
          description: Immutable JSON snapshot vendor row at time of receipt
        own_snapshot: { type: object, nullable: true }
        total_without_vat: { type: number }
        total_vat: { type: number }
        total_with_vat: { type: number }
        rounding: { type: number }
        advance_paid_amount: { type: number }
        amount_to_pay: { type: number }
        payment_currency_id: { type: integer, nullable: true }
        payment_exchange_rate: { type: number, nullable: true }
        paid_amount_payment_ccy: { type: number, nullable: true }
        paid_amount_invoice_ccy: { type: number, nullable: true }
        exchange_diff_base:
          type: number
          nullable: true
          description: "FX gain/loss v base ccy (CZK)"
        status: { type: string, enum: [draft, received, booked, paid, cancelled] }
        booked_at: { type: string, format: date-time, nullable: true }
        paid_at: { type: string, format: date, nullable: true }
        cancelled_at: { type: string, format: date-time, nullable: true }
        pdf_path:
          type: string
          nullable: true
          description: "Relative path within archive_storage"
        pdf_hash: { type: string, nullable: true, description: "SHA-256 hex" }
        pdf_size_bytes: { type: integer, nullable: true }
        pdf_original_name: { type: string, nullable: true }
        pdf_uploaded_at: { type: string, format: date-time, nullable: true }
        vat_classification_code: { type: string, nullable: true }
        vat_overrides:
          type: array
          nullable: true
          description: "Per-sazba override rekapitulace DPH (§73; migrace 0086). Zapéká se do řádkových totálů."
          items:
            type: object
            properties:
              rate: { type: number }
              base: { type: number }
              vat:  { type: number }
        extraction_warning: { type: string, nullable: true, description: "Varování z AI/automatické extrakce dokladu (rozdíl součtů apod.)" }
        idoklad_id: { type: integer, nullable: true, description: "ID v iDoklad (jen u importovaných)" }
        fakturoid_id: { type: integer, nullable: true, description: "ID ve Fakturoid (jen u importovaných)" }
        created_by: { type: integer }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        vendor_company_name: { type: string }
        vendor_ic: { type: string, nullable: true }
        vendor_dic: { type: string, nullable: true }
        vendor_main_email: { type: string, nullable: true }
        linked_advance:
          type: object
          nullable: true
          description: "Brief zálohy, kterou tento doklad vyúčtovává ({ id, varsymbol, document_kind, status, total_with_vat, currency })"
        advance_link_suggestion: { type: object, nullable: true, description: "Brief AI-navržené zálohy k propojení" }
        settled_by: { type: object, nullable: true, description: "Jen u zálohy — brief finální faktury, která ji vyúčtovává" }
        has_advance_candidates: { type: boolean }
        has_settlement_candidates: { type: boolean }
        items:
          type: array
          items: { $ref: '#/components/schemas/PurchaseInvoiceItem' }
        vat_breakdown:
          type: array
          items:
            type: object
            properties:
              vat_rate: { type: number }
              without_vat: { type: number }
              vat: { type: number }
              with_vat: { type: number }
        totals:
          type: object
          properties:
            without_vat: { type: number }
            vat: { type: number }
            with_vat: { type: number }
            rounding: { type: number }
            advance_paid_amount: { type: number }
            amount_to_pay: { type: number }

    PurchaseInvoicePayload:
      type: object
      required: [vendor_id, vendor_invoice_number, issue_date, due_date, currency_id, items]
      properties:
        vendor_id: { type: integer }
        vendor_invoice_number: { type: string, maxLength: 50 }
        varsymbol: { type: string, nullable: true, maxLength: 20 }
        document_kind: { type: string, enum: [invoice, receipt, credit_note, advance], default: invoice }
        issue_date: { type: string, format: date }
        tax_date: { type: string, format: date, nullable: true }
        due_date: { type: string, format: date }
        received_at: { type: string, format: date }
        currency_id: { type: integer }
        exchange_rate: { type: number, nullable: true }
        exchange_rate_date: { type: string, format: date, nullable: true }
        exchange_rate_source: { type: string, enum: [cnb, manual, idoklad, fakturoid], default: cnb }
        reverse_charge: { type: boolean, default: false }
        prices_include_vat: { type: boolean, default: false, description: "Ceny položek zadané včetně DPH (DPH shora koeficientem)." }
        expense_category_id: { type: integer, nullable: true, description: "Kategorie nákladu. Když chybí, na create se odvodí z default kategorie dodavatele." }
        vat_overrides:
          type: array
          nullable: true
          description: "Per-sazba override rekapitulace DPH (§73). Zapisuje se jen když je klíč přítomen; zapéká se do řádkových totálů."
          items:
            type: object
            properties:
              rate: { type: number }
              base: { type: number }
              vat:  { type: number }
        rounding: { type: number, description: "Jen PUT — zaokrouhlení dokladu; zapisuje se jen když je klíč přítomen." }
        vat_deduction: { type: string, enum: [full, none, proportional], default: full, description: "Nárok na odpočet DPH (none → mimo Knihu DPH/DPHDP3/KH; proportional → krácený §75 dle vat_deduction_percent)." }
        vat_deduction_percent: { type: number, format: float, minimum: 0, maximum: 100, default: 100, description: "Procento odpočtu pro proportional (§75)." }
        tax_deductible: { type: boolean, default: true, description: "Daňová uznatelnost pro daň z příjmů (nezávislé na DPH)." }
        is_fixed_asset:
          type: boolean
          default: false
          description: "Doklad za dlouhodobý majetek — propíše se do ř. 47 DPHDP3 jako doplňující údaj."
        language: { type: string, enum: [cs, en], default: cs }
        note_above_items: { type: string, nullable: true }
        note_below_items: { type: string, nullable: true }
        advance_paid_amount: { type: number, default: 0 }
        payment_currency_id: { type: integer, nullable: true }
        payment_exchange_rate: { type: number, nullable: true }
        paid_amount_payment_ccy: { type: number, nullable: true }
        paid_amount_invoice_ccy: { type: number, nullable: true }
        exchange_diff_base: { type: number, nullable: true }
        vat_classification_code: { type: string, nullable: true }
        items:
          type: array
          items: { $ref: '#/components/schemas/PurchaseInvoiceItemPayload' }

    InboxScanResult:
      type: object
      properties:
        created: { type: integer }
        skipped: { type: integer }
        failed: { type: integer }
        dry_run: { type: boolean }
        inbox_dir: { type: string }
        details:
          type: array
          items:
            type: object
            properties:
              file: { type: string }
              status:
                type: string
                enum: [created, skipped, failed, rejected, mapper_pending, config_missing, inbox_missing, limit_reached]
              reason: { type: string, nullable: true }
              purchase_invoice_id: { type: integer, nullable: true }
              isdoc_invoice_count: { type: integer, nullable: true }
              supplier_ic: { type: string, nullable: true }

    Car:
      type: object
      properties:
        id: { type: integer, format: int64 }
        registration: { type: string }
        name: { type: string, nullable: true }
        brand: { type: string, nullable: true }
        model: { type: string, nullable: true }
        vin: { type: string, nullable: true }
        fuel_type: { type: string, nullable: true, enum: [diesel, petrol, lpg, cng, electric, hybrid, other] }
        odometer_start: { type: integer, nullable: true }
        odometer_start_date: { type: string, format: date, nullable: true }
        is_default: { type: boolean }
        is_archived: { type: boolean }
        note: { type: string, nullable: true }
        last_odometer: { type: integer, nullable: true, description: Poslední známý stav tachometru (z jízd) }
        trips_count: { type: integer, nullable: true }
        fuelings_count: { type: integer, nullable: true }
        created_at: { type: string, format: date-time }
    CarInput:
      type: object
      required: [registration]
      properties:
        registration: { type: string, maxLength: 20 }
        name: { type: string, nullable: true }
        brand: { type: string, nullable: true }
        model: { type: string, nullable: true }
        vin: { type: string, nullable: true }
        fuel_type: { type: string, nullable: true, enum: [diesel, petrol, lpg, cng, electric, hybrid, other] }
        odometer_start: { type: integer, nullable: true }
        odometer_start_date: { type: string, format: date, nullable: true }
        is_default: { type: boolean }
        is_archived: { type: boolean }
        note: { type: string, nullable: true }
    Trip:
      type: object
      properties:
        id: { type: integer, format: int64 }
        car_id: { type: integer }
        car_registration: { type: string, nullable: true }
        trip_date: { type: string, format: date }
        time_start: { type: string, nullable: true }
        time_end: { type: string, nullable: true }
        odometer_start: { type: integer, nullable: true }
        odometer_end: { type: integer, nullable: true }
        distance_km: { type: number }
        category_id: { type: integer, nullable: true }
        category_label: { type: string, nullable: true }
        category_is_private: { type: boolean, nullable: true }
        purpose: { type: string, nullable: true }
        origin: { type: string, nullable: true }
        destination: { type: string, nullable: true }
        note: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
    TripInput:
      type: object
      required: [car_id, trip_date]
      properties:
        car_id: { type: integer }
        trip_date: { type: string, format: date }
        time_start: { type: string, nullable: true }
        time_end: { type: string, nullable: true }
        odometer_start: { type: integer, nullable: true }
        odometer_end: { type: integer, nullable: true }
        distance_km: { type: number, nullable: true, description: "Prázdné = dopočítá se z tachometru (konec − začátek)" }
        category_id: { type: integer, nullable: true }
        purpose: { type: string, nullable: true }
        origin: { type: string, nullable: true }
        destination: { type: string, nullable: true }
        note: { type: string, nullable: true }
    Fueling:
      type: object
      properties:
        id: { type: integer, format: int64 }
        car_id: { type: integer, nullable: true }
        car_registration: { type: string, nullable: true }
        fueled_date: { type: string, format: date }
        fueled_time: { type: string, nullable: true }
        fuel_type: { type: string, nullable: true }
        quantity: { type: number, nullable: true }
        unit: { type: string }
        unit_price: { type: number, nullable: true }
        amount_without_vat: { type: number, nullable: true }
        amount_vat: { type: number, nullable: true }
        amount_with_vat: { type: number }
        currency: { type: string }
        odometer: { type: integer, nullable: true }
        station: { type: string, nullable: true }
        vendor_id: { type: integer, nullable: true }
        vendor_name: { type: string, nullable: true }
        source: { type: string, enum: [manual, invoice, axigon, axigon_ai, import] }
        source_purchase_invoice_id: { type: integer, nullable: true }
        source_invoice_number: { type: string, nullable: true }
        receipt_number: { type: string, nullable: true }
        note: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
    FuelingInput:
      type: object
      required: [fueled_date, amount_with_vat]
      properties:
        car_id: { type: integer, nullable: true }
        fueled_date: { type: string, format: date }
        fueled_time: { type: string, nullable: true }
        fuel_type: { type: string, nullable: true }
        quantity: { type: number, nullable: true }
        unit: { type: string }
        unit_price: { type: number, nullable: true }
        amount_with_vat: { type: number }
        currency: { type: string }
        odometer: { type: integer, nullable: true }
        station: { type: string, nullable: true }
        vendor_id: { type: integer, nullable: true }
        note: { type: string, nullable: true }
    TripCategory:
      type: object
      properties:
        id: { type: integer }
        code: { type: string }
        label: { type: string }
        is_private: { type: boolean }
        display_order: { type: integer }
        is_archived: { type: boolean }
        trips_count: { type: integer, nullable: true }
    LogbookSummary:
      type: object
      properties:
        year: { type: integer }
        available_years: { type: array, items: { type: integer } }
        vehicles: { type: array, items: { $ref: '#/components/schemas/SummaryVehicle' } }
        totals: { $ref: '#/components/schemas/SummaryTotals' }
        monthly: { $ref: '#/components/schemas/MonthlyKm' }
    MonthlyKm:
      type: object
      description: Najeté km po měsících (12 hodnot, leden..prosinec) — vybraný rok vs předchozí.
      properties:
        year: { type: integer }
        prev_year: { type: integer }
        current: { type: array, items: { type: number } }
        previous: { type: array, items: { type: number } }
    SummaryVehicle:
      type: object
      properties:
        car_id: { type: integer }
        registration: { type: string }
        name: { type: string, nullable: true }
        trips_count: { type: integer }
        km: { type: number }
        business_km: { type: number }
        private_km: { type: number }
        uncategorized_km: { type: number }
        private_ratio: { type: number }
        business_ratio: { type: number }
        odometer_start: { type: integer, nullable: true }
        odometer_end: { type: integer, nullable: true }
        fuel_count: { type: integer }
        liters: { type: number }
        liters_count: { type: integer }
        liters_incomplete: { type: boolean }
        kwh: { type: number, description: Charged energy (kWh) for electric/PHEV vehicles. }
        kwh_count: { type: integer }
        kwh_incomplete: { type: boolean }
        fuel_cost: { type: number }
        avg_consumption: { type: number, nullable: true, description: Fuel consumption l/100 km. }
        avg_consumption_kwh: { type: number, nullable: true, description: Electric consumption kWh/100 km. }
        continuity_issues: { type: integer }
        continuity_detail:
          type: array
          items:
            type: object
            properties:
              prev_date: { type: string, format: date }
              prev_end: { type: integer }
              date: { type: string, format: date }
              start: { type: integer }
              gap: { type: integer }
        pausal_months: { type: integer }
        pausal_rate: { type: integer }
        pausal_year: { type: integer }
    SummaryTotals:
      type: object
      properties:
        vehicles_count: { type: integer }
        trips_count: { type: integer }
        km: { type: number }
        business_km: { type: number }
        private_km: { type: number }
        uncategorized_km: { type: number }
        private_ratio: { type: number }
        fuel_count: { type: integer }
        liters: { type: number }
        kwh: { type: number, description: Total charged energy (kWh). }
        fuel_cost: { type: number }
        avg_consumption: { type: number, nullable: true }
        avg_consumption_kwh: { type: number, nullable: true }
        liters_incomplete: { type: boolean }
        kwh_incomplete: { type: boolean }
        continuity_issues: { type: integer }
