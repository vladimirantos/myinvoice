{% extends '_layout.html.twig' %}

{% block content %}
<h1 style="margin:0 0 16px;font-size:22px;font-weight:600;color:#15131D;">
  {% if is_test %}<span style="color:#E8A547;">[TEST] </span>{% endif %}{% if is_reminder %}<span style="color:#E8A547;">Připomínka</span> — {% endif %}Dobrý den,
</h1>

{% if is_reminder %}
<p style="margin:0 0 16px;padding:10px 14px;background:#FDF6E9;border-left:3px solid #E8A547;border-radius:4px;">
  Tohle je <strong>upomínka</strong> — již jsme Vás žádali o schválení tohoto výkazu, ale zatím jsme nedostali odpověď.
</p>
{% endif %}

<p style="margin:0 0 16px;">
  v příloze posíláme <strong>výkaz víceprací</strong> za zakázku
  {% if invoice.project_name %}<strong>{{ invoice.project_name }}</strong>{% endif %}
  ke schválení{% if invoice.client_company_name %} pro <strong>{{ invoice.client_company_name }}</strong>{% endif %}.
</p>

<table role="presentation" cellpadding="0" cellspacing="0" style="width:100%;border-collapse:collapse;background:{{ accent_soft }};border-radius:8px;margin:16px 0;">
  <tr>
    <td style="padding:16px 20px;">
      <table role="presentation" cellpadding="0" cellspacing="0" style="width:100%;font-size:14px;color:#403B52;">
        {% if work_report %}
        <tr>
          <td style="padding:4px 0;color:#7A748C;width:42%;">Výkaz</td>
          <td style="padding:4px 0;font-weight:600;color:#15131D;">{{ work_report.title }}</td>
        </tr>
        <tr>
          <td style="padding:4px 0;color:#7A748C;">Hodin celkem</td>
          <td style="padding:4px 0;font-family:monospace;font-weight:600;">{{ work_report.total_hours|number_format(2, ',', ' ') }} h</td>
        </tr>
        <tr>
          <td style="padding:8px 0 0;color:#7A748C;border-top:1px solid #E5E0F4;">K vyúčtování</td>
          <td style="padding:8px 0 0;border-top:1px solid #E5E0F4;font-size:18px;font-weight:700;color:{{ accent }};font-family:monospace;">
            {{ work_report.total_amount|number_format(2, ',', ' ') }} {{ invoice.currency }}
          </td>
        </tr>
        {% endif %}
      </table>
    </td>
  </tr>
</table>

<p style="margin:16px 0;">
  Po schválení Vám obratem pošleme fakturu — <strong>vše je automatizované</strong>,
  není potřeba na nic odpovídat. Stačí kliknout na tlačítko níže.
</p>

<table role="presentation" cellpadding="0" cellspacing="0" border="0" align="center" style="margin:28px auto;">
  <tr>
    {# Bulletproof button: bgcolor atribut + mso-padding-alt kvůli Outlooku (Word engine
       ignoruje padding/zaoblení na <a> i CSS background na <td>). #}
    <td align="center" bgcolor="#15803D" style="background:#15803D;border-radius:10px;mso-padding-alt:24px 64px;">
      <a href="{{ approval_url }}"
         style="display:inline-block;padding:24px 64px;font-family:Arial,Helvetica,sans-serif;font-size:18px;font-weight:700;line-height:1;color:#ffffff;text-decoration:none;border-radius:10px;">✓ Schválit vícepráce</a>
    </td>
  </tr>
</table>

<p style="margin:24px 0 8px;font-size:13px;color:#7A748C;text-align:center;">
  Nebo odkaz zkopírujte do prohlížeče:<br>
  <a href="{{ approval_url }}" style="color:{{ accent }};word-break:break-all;font-family:monospace;font-size:12px;">{{ approval_url }}</a>
</p>

<p style="margin:16px 0;font-size:13px;color:#5A5470;">
  Detailní výpis víceprací najdete v PDF příloze tohoto emailu (Vykaz-{{ invoice.varsymbol_or_id }}.pdf).
  Na schvalovací stránce máte k dispozici i možnost zamítnutí s důvodem.
</p>

{% if is_test %}
<p style="margin:24px 0 0;padding:12px;background:#FDF6E9;border-left:2px solid #E8A547;font-size:13px;color:#8B6420;">
  <strong>Tohle je TEST email.</strong> Klient ho nedostal a token pro schválení nebyl vygenerován.
</p>
{% endif %}
{% endblock %}
