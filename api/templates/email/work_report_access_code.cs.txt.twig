{% extends '_layout.txt.twig' %}

{% block content %}
Ověřovací kód
-------------

Dobrý den,

pro zobrazení náhledu výkazu práce zadejte tento ověřovací kód:

    {{ code }}

Kód je platný {{ expiresIn|default('15 min') }} a lze ho použít jen jednou. Po jeho zadání si Vás prohlížeč zapamatuje a příště se kód už nevyžaduje.

Pokud jste o náhled nežádali, tento e-mail ignorujte.
{% endblock %}
