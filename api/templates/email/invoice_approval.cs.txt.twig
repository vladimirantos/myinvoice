{% extends '_layout.txt.twig' %}

{% block content %}
{% if is_test %}[TEST] {% endif %}{% if is_reminder %}[PŘIPOMÍNKA] {% endif %}Dobrý den,
------------

{% if is_reminder %}>>> UPOMÍNKA: již jsme Vás žádali o schválení tohoto výkazu, ale zatím jsme nedostali odpověď. <<<

{% endif %}

v příloze posíláme výkaz víceprací{% if invoice.project_name %} za zakázku „{{ invoice.project_name }}"{% endif %}{% if invoice.client_company_name %} pro {{ invoice.client_company_name }}{% endif %} ke schválení.

{% if work_report %}Výkaz:           {{ work_report.title }}
{% if work_report.total_hours > 0 %}Hodin celkem:    {{ work_report.total_hours|number_format(2, ',', ' ') }} h
{% endif %}{% if work_report.material_total > 0 %}{{ work_report.material_title|default('Materiál') }}:        {{ work_report.material_total|number_format(2, ',', ' ') }} {{ invoice.currency }}
{% endif %}K vyúčtování:    {{ (work_report.total_amount + work_report.material_total)|number_format(2, ',', ' ') }} {{ invoice.currency }}
{% endif %}

Po schválení Vám obratem pošleme fakturu — vše je automatizované,
není potřeba na nic odpovídat. Schválit/zamítnout můžete na adrese:

  {{ approval_url }}

Detailní výpis víceprací je v PDF příloze (Vykaz-{{ invoice.varsymbol_or_id }}.pdf).

{% if is_test %}=== TEST EMAIL — klient ho nedostal a token nebyl vygenerován. ==={% endif %}
{% endblock %}
