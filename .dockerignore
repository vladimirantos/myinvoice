.git
.gitignore
.vscode
.claude
.idea

# Root-level docs (manual/*.md MUST stay — generateManualHtml.php čte z nich)
/README.md
/CHANGELOG.md
/source

# Will be rebuilt inside image
api/vendor
web/node_modules
web/dist
manual/generated
manual/manual.pdf

# Local-only — secrets, NESMÍ se dostat do image (vše se čte z hostu:
# .env přes compose var-substituci, cfg.docker.php přes read-only bind-mount).
cfg.php
cfg.local.php
cfg.docker.php
.env
.env.*
log
storage
private
aspnet_client

# Soukromé naskenované faktury pro jednorázový import — nikdy do image
faktury

# Web server configs (Docker uses Apache directly via .htaccess)
web.config

# Jednorázový externí migrační nástroj (Money S3) — runtime ho nespouští, do image nepatří
tools/money-s3-import

# Build artefacts
*.log
.DS_Store
Thumbs.db
